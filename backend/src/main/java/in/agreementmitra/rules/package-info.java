/**
 * Rules module (FUTURE): multi-state legal-logic rules engine that generates
 * jurisdiction-correct agreements. Intended implementation is Drools
 * (JVM-native) — the main reason the backend is on the JVM. Stub for now.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Rules")
package in.agreementmitra.rules;
