/**
 * Identity module (FUTURE): Aadhaar/PAN/DigiLocker KYC and fraud/forgery detection. Stub package so
 * the module boundary exists from day one. Any PII flow added here must be redacted in logs and
 * specced via OpenSpec.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Identity")
package in.agreementmitra.identity;
