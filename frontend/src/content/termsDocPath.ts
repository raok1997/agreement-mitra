/**
 * Where the generated counsel-facing copy of the terms lives, relative to `frontend/`. Shared by the
 * generator (scripts/render-terms.ts) and the staleness test so the two cannot disagree about which
 * file is the one being guarded.
 */
export const TERMS_DOC_PATH = "../docs/TERMS-OF-SERVICE.md";
