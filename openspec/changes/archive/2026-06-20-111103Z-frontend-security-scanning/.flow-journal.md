# Flow journal — frontend-security-scanning (CR-6)

## Propose — 2026-06-20  [✅]
Outcome: Drafted the full proposal set (proposal/design/specs/tasks) for a frontend npm dependency-vulnerability gate that mirrors the backend's CR-4 OSV gate. `openspec validate` passes.
Decisions:
- Scanner = OSV-Scanner (not npm audit) — user-chosen pre-draft, for one-tool/one-mental-model parity with backend + first-class reason+expiry suppressions.
- Threshold = fail-on-any-unsuppressed (not a CVSS/severity cutoff) — user-chosen, matches backend posture; appropriate for legal/identity infra.
- Home = an npm script `security:scan` (local-only), NOT wired into `npm run build`; always-on CI deferred to CR-7. Mirrors backend gate being a runnable task before CI.
- Config at `frontend/osv-scanner.toml`; empty baseline (graph clean today, npm audit → 0 vulns).
- Test-exempt (pure build-tooling/config, no `src/` changes) — one-line note atop tasks.md per dev-policy.
Halts: none.
Follow-up CRs: none new (CR-7 ci-pipeline already on roadmap will promote this to CI).

## Review — 2026-06-20  [✅]
Outcome: Two-round multi-persona review. Round 1: 1 Critical, 6 Major, 4 Minor. Folded all minimal findings; halted on ONE genuine scope decision. Round 2: all resolved, no new Critical/Major.
Decisions:
- Scan scope = WHOLE lockfile (dev+prod, transitives) — user-confirmed. Deliberately DIVERGES from backend's exclude-build-tooling scope; rationale: npm devDeps are #1 supply-chain surface and ~95% of this graph (mirroring backend would cover ~only `vue`). Recorded as design Decision 6.
- Fail-closed mechanism = cross-platform Node shim `frontend/scripts/security-scan.mjs` (not a shell guard — wouldn't fire under cmd.exe).
- Pinned exact command: `osv-scanner scan --config=osv-scanner.toml --lockfile=package-lock.json`, run from `frontend/`.
- Frontend-specific toml header (no backend Gradle/SpotBugs boilerplate).
- Verification: non-destructive throwaway fixture lockfile seeding a transitive CVE (fallback: direct-pin); assert fail-closed message content.
- Lockfile-staleness + no-build-coupling now explicit eyes-open risks; freshness check deferred to CR-7.
Halts: scan-scope (dev vs prod) — user chose whole lockfile. Resolved.
Follow-up CRs: none new.

## Apply — 2026-06-20  [✅]
Outcome: Implemented the frontend OSV gate. All 8 tasks done; all three verifications pass live (clean=exit0, transitive-fixture=exit1, fail-closed-PATH-stripped=exit1 with install hint). No app/src changes.
Decisions: none beyond design. OSV-Scanner 2.4.0 confirmed; real lockfile = 385 packages, 0 vulns.
Halts: none.
Follow-up CRs: none.
Modified files:
- frontend/osv-scanner.toml (NEW — empty frontend-specific baseline)
- frontend/scripts/security-scan.mjs (NEW — cross-platform Node shim)
- frontend/package.json (added `security:scan` script)
- CLAUDE.md (Testing & Scanning — frontend gate documented; "Not yet covered" trimmed to CI only)
- openspec/changes/frontend-security-scanning/* (artifacts + tasks marked done)
Verification: 3.2 used a throwaway /tmp fixture (parent-pkg → minimatch@3.0.4 transitive) → OSV flagged 4 advisories, exit 1; fixture removed; real package-lock.json untouched (git status confirms only intended changes).

## Validate — 2026-06-20  [✅]
Outcome: openspec-validate (in subagent) = valid; 11/11 requirements verified, 0 gaps (Critical/Major/Minor all none). Diff scope matches proposal Impact exactly; no stray/src changes. No Fix stage needed.
Decisions: none. Halts: none. Follow-up CRs: none.

## Manual-test gate — 2026-06-20  [⛔]
Outcome: Halting for user manual test before archive (hard gate). Implementation done, validate clean.
Halts: Awaiting user confirmation. Proposed test recipe — from `frontend/`: (1) `npm run security:scan` → expect "No issues found", exit 0; (2) fail-closed: `env PATH="$(dirname "$(command -v node)")" node scripts/security-scan.mjs` → expect install-hint message + non-zero exit; (3) optional fail-on-finding: point a throwaway fixture lockfile with a known-CVE dep at the scanner → expect non-zero exit naming the advisory.
