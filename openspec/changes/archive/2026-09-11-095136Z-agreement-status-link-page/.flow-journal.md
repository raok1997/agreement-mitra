# Flow journal — agreement-status-link-page

## propose — 2026-09-11T06:53:08.125Z  [✅]
Outcome: Reworked the existing proposal so the emailed link lands on a status page and the terms freeze moves from finalisation to stamping, while editing stays an authenticated, owner-only action. The anonymous edit route, its rate limiter, audit table and SecurityConfig delta were removed.
Decisions: Edits are owner-only; the link confers read plus a claim-to-edit path (design D3/D4). Freeze at STAMPED keeps its rationale (duty is a function of rent+term; certificate bought against one document). Progress gains termsOpen + editable so the page never infers. Binding claim to the initiator is NOT folded in: the system records no initiator, identity is Google-email only, mobile-otp-auth is at 0/40 tasks; registered as a follow-up instead. Residual first-claimant-wins risk stated explicitly.
Halts: none
Follow-up CRs: `claim-bound-to-initiator` — capture an initiator contact at draft time and allow claim only by a matching verified identity (depends on mobile-otp-auth); `notify-parties-on-post-order-edit` — email every party when terms change after the order is placed; register rows to be added at task 8.2 before 7a
Modified files:
openspec/changes/agreement-status-link-page/proposal.md
openspec/changes/agreement-status-link-page/design.md
openspec/changes/agreement-status-link-page/tasks.md
openspec/changes/agreement-status-link-page/specs/agreement-management/spec.md
openspec/changes/agreement-status-link-page/specs/agreement-recovery/spec.md
openspec/changes/agreement-status-link-page/specs/signing-request/spec.md
openspec/changes/agreement-status-link-page/specs/agreement-status-view/spec.md

## review (round 1) — 2026-09-11T07:12:38.583Z  [⛔]
Outcome: Three-persona review (architect, engineer, security) over the reworked artifacts with a code grounding map and a 41-row coverage matrix (0 UNMAPPED). It found that the artifacts promise a document regenerated from corrected terms but never say how: the only regeneration route keeps the old freeze, so every post-finalisation edit would strand a paid order; and moving that route naively would open an anonymous PDF-overwrite during the paid-to-stamped window. Also an edit/stamp-intake race, an archive-order dependency on zoop-aadhaar-esign, and that progress() cannot see the owner today.
Decisions: Coverage matrix written into tasks.md. Task 3.5 gained the 400 case. No artifact fixes applied yet — the four Criticals need the user's call on regeneration strategy, the intake guard, STAMP_FAILED, and notify-parties scope.
Halts: Halted for decisions: (1) regenerate server-side inside owner-only update and leave POST /document and /draft on the finalisation freeze [recommended] vs move DraftService to the new predicate with owner scoping; (2) add a draft-key/terms_changed_at re-check to the STAMPED transition (touches stamp intake) [recommended]; (3) STAMP_FAILED/FAILED/EXPIRED stay closed, spec reworded to 'once the request has left PDF_GENERATED' [recommended]; (4) fold notify-all-parties-on-post-order-edit via existing DraftDeliveryService [recommended] vs keep as follow-up; (5) record archive-after-zoop-aadhaar-esign dependency (zoop at 60/61 tasks) [recommended] vs carry the ADDED text.
Follow-up CRs: none
Modified files:
openspec/changes/agreement-status-link-page/tasks.md

## review (round 1 fixes) — 2026-09-11T07:32:12.655Z  [✅]
Outcome: Applied the user's five decisions to the artifacts: the edit now regenerates the draft inside its own transaction while the public draft routes stay frozen at finalisation; the STAMPED transition refuses a superseded draft (new estamp-intake requirement); terminal-failure states stay closed and the rule is worded as 'until the request leaves PDF_GENERATED'; every party is emailed the regenerated draft and the edit is attributed; archive is sequenced after zoop-aadhaar-esign. Also folded the Majors and Minors: tri-state editability plus an explicit stage enum and terminal flag, ownerOf lookup, explicit Save-to-my-account click with a validated return id consumed by App.vue, polling floor and 404 handling, unauthenticated PUT reworded to chain rejection, RENAMED blocks for both retained headings, the breaking-test list, StaffAgreementView widening with a timestamp, ToS wording 'until the stamp certificate is attached', and a 56-row coverage matrix (0 UNMAPPED). Structural validation passes.
Decisions: User chose all five recommendations. Notify-parties moved from follow-up into scope (D7). problemType on updateAgreement folded as the narrow slice this page needs; the register row for the other sites stays.
Halts: none
Follow-up CRs: none
Modified files:
openspec/changes/agreement-status-link-page/proposal.md
openspec/changes/agreement-status-link-page/design.md
openspec/changes/agreement-status-link-page/tasks.md
openspec/changes/agreement-status-link-page/specs/agreement-management/spec.md
openspec/changes/agreement-status-link-page/specs/agreement-recovery/spec.md
openspec/changes/agreement-status-link-page/specs/signing-request/spec.md
openspec/changes/agreement-status-link-page/specs/agreement-status-view/spec.md
openspec/changes/agreement-status-link-page/specs/estamp-intake/spec.md

## review (round 2) — 2026-09-11T07:41:41.588Z  [⛔]
Outcome: Round 2 verified most round-1 fixes but found the stamp-intake guard cannot work as designed: every draft is stored at the same key (drafts/<id>.pdf), so comparing keys never detects a superseded draft, and the guard must also run under the agreement row lock. It also found that the existing edit form sends no party contacts, so a post-payment edit would wipe both parties' contacts and bypass the contacts-at-payment freeze; that the in-transaction render holds a row lock across a 30 s Gotenberg call; and that payment-processing still says terms freeze at order placement.
Decisions: none
Halts: Halted for decisions: (1) add a draft_generation counter (V20) bumped on every regeneration, captured at intake read and compared under findByIdForUpdate in markStamped [recommended]; (2) post-payment edit keeps party count/roles and contacts server-preserved — terms and party details change, who-is-notified does not; 409 on a party-set change [recommended] vs notify union of old+new contacts; (3) two-transaction edit — tx1 terms+attribution+bump generation+clear pin, render outside the lock, tx2 re-lock+verify+attach+pin; render failure -> retry by PUT, DocumentRenderException mapped to 503 [recommended] vs render-first from request data; (4) add MODIFIED deltas for payment-processing 'Confirmed contacts are saved' and estamp-intake 'Staff have a console listing orders' (state-stamp-duty-quoting at 0/55 also modifies the latter; it rebases) [recommended]; (5) post-order edit may not change (state,type) — 409 [recommended]; (6) claim-after-SIGNED lockout: register as follow-up [recommended] vs gate claim on terms-open now.
Follow-up CRs: none
Modified files:
openspec/changes/agreement-status-link-page/tasks.md

## propose (re-scope) — 2026-09-11T07:52:51.044Z  [✅]
Outcome: Rewrote the change down to the status page alone at the user's direction: post-payment editing is dropped as a feature. Backend is two additive fields on progress (a FulfilmentStage enum and a terminal flag); frontend is the status view, the route fix and polling. No migration, no write path, no SecurityConfig, no claim from the link, no ToS change. Two spec deltas (signing-request MODIFIED, agreement-status-view ADDED), 15 tasks, 23 scenarios, 0 UNMAPPED.
Decisions: The freeze-move was the source of every review finding across two rounds (regeneration, intake race, notification, contacts, attribution, queue flag, cross-capability spec drift). Rather than shrink it, the user chose to remove it entirely and add it later if needed. Its design work is preserved in git history and the journal as prior art for the follow-up.
Halts: none
Follow-up CRs: `terms-correctable-until-stamping` — owner may correct terms after payment and before the stamp; register row to be added at task 3.1 before 7a
Modified files:
openspec/changes/agreement-status-link-page/proposal.md
openspec/changes/agreement-status-link-page/design.md
openspec/changes/agreement-status-link-page/tasks.md
openspec/changes/agreement-status-link-page/specs/signing-request/spec.md
openspec/changes/agreement-status-link-page/specs/agreement-status-view/spec.md

## review (round 1 after re-scope) — 2026-09-11T07:59:50.790Z  [✅]
Outcome: Three-persona review of the re-scoped change found no Criticals. Every finding was minimal and in-scope and was folded: the signed-document download now fetches with the session header (a bare link 404s for a signed-in owner), progress gains signedDocumentReady because SIGNED can precede the stored PDF, milestone rules are explicit sets with an ordering rule for order-before-payment, the page shows party name and role only, polling moves to an injectable usePolling composable with a 5-minute cap and a visibility scenario, a unit no-leak test is written rather than 'extended', and the integration test states how each FSM state is produced. 30 scenarios, 0 UNMAPPED. Structural validation passes. A confirmatory round 2 is running.
Decisions: No halts. Register widening for signing-auth to name the anonymous reads folded into task 3.1.
Halts: none
Follow-up CRs: none
Modified files:
openspec/changes/agreement-status-link-page/proposal.md
openspec/changes/agreement-status-link-page/design.md
openspec/changes/agreement-status-link-page/tasks.md
openspec/changes/agreement-status-link-page/specs/signing-request/spec.md
openspec/changes/agreement-status-link-page/specs/agreement-status-view/spec.md

## review (round 2 confirmatory) — 2026-09-11T08:40:51.158Z  [✅]
Outcome: The confirmatory subagent was killed by a session rate limit, so the spine verified its seven checkpoints directly against the code: the blob-download pattern in CaptureForm, signedPdfKeyForAgreement on the query adapter, InviteeStatus and SIGNING_ORDER semantics, orderStatus null meaning payment never started, the JdbcTemplate in SigningProgressApiIntegrationTest, the signed_pdf_key column on signing_request, and the delta being a superset of zoop's ADDED text. All hold. Review closed after two rounds on the re-scoped change; nothing outstanding.
Decisions: Round 2 done in-spine rather than re-spending a subagent on a rate-limited session.
Halts: none
Follow-up CRs: none
Modified files:
openspec/changes/agreement-status-link-page/.flow-journal.md

## apply — 2026-09-11T08:51:15.586Z  [✅]
Outcome: Implemented the status page end to end. Backend: a FulfilmentStage enum and three additive fields (stage, terminal, signedDocumentReady) on the progress view, with unit, shape and integration tests. Frontend: a signingProgress API module (header-authenticated download), a usePolling composable, the AgreementStatus view, and the App route that now keeps the link in the address bar. Full backend check green: 969 tests, 0 skipped, ModularityTests green, 62 s wall-clock (budget 3 min). Frontend: 217 tests green, vue-tsc clean, vite build succeeds. Every task is ticked except the manual-test gate (3.2).
Decisions: npm run build is blocked at security:scan by the pre-existing vitest 3.2.7 advisory GHSA-82fw-gwwq-j7x9 (register row frontend-dev-dep-refresh) and lint's only two errors are that row's security-scan.mjs globals; both predate this change and are not folded. Task 2.9 is ticked on lint + test + vue-tsc + vite build passing, with the scan gate recorded here. The visibility floor is pinned in the usePolling unit test rather than the component test. Test fixture ids are alphanumeric because the PII edit guard treats 12 consecutive digits as Aadhaar-like.
Halts: none
Follow-up CRs: `terms-correctable-until-stamping` — registered in docs/ROADMAP.md with the prior art from review rounds 1-2; `signing-auth` — row widened to name the three anonymous reads the page polls
Modified files:
backend/src/main/java/in/agreementmitra/signing/FulfilmentStage.java
backend/src/main/java/in/agreementmitra/signing/api/SigningProgressResponse.java
backend/src/main/java/in/agreementmitra/signing/signingrequest/SigningRequestPersistence.java
backend/src/main/java/in/agreementmitra/signing/signingrequest/SigningRequestService.java
backend/src/test/java/in/agreementmitra/signing/FulfilmentStageTest.java
backend/src/test/java/in/agreementmitra/signing/api/SigningProgressResponseShapeTest.java
backend/src/test/java/in/agreementmitra/signing/signingrequest/SigningProgressApiIntegrationTest.java
frontend/src/api/signingProgress.ts
frontend/src/composables/usePolling.ts
frontend/src/composables/usePolling.test.ts
frontend/src/views/AgreementStatus.vue
frontend/src/views/AgreementStatus.test.ts
frontend/src/App.vue
frontend/src/App.test.ts
docs/ROADMAP.md
openspec/changes/agreement-status-link-page/tasks.md

## fix — 2026-09-11T09:06:42.019Z  [✅]
Outcome: Folded every finding from the three validate gates. Bugs fixed: a claimed-meanwhile 404 was recognised only from the progress read (now either read, via allSettled, with partial-failure tolerance); a slow older poll could roll a fresh PAID back (read-sequence guard); the blob URL was revoked before the download could start (deferred); a stop/start on the polling composable could leave two loops reading (generation token, cancellable wait); the page invited payment on a terminal agreement; role labels differed between the terms and the timeline; date-only strings were parsed as UTC; the mid-session unavailable message diverged from the shell's and showed a sign-in button to a signed-in viewer (shared copy in linkCopy.ts, session-aware); a history move onto another link showed the previous agreement. Added: payment read skipped once settled, minimal nav on the link page, a signingProgress API test asserting the Bearer header. Register rows claim-bound-to-initiator and progress-read-hot-path added. Re-checks: 230 frontend tests, vue-tsc, vite build green; backend targeted tests 5 + 2 + 7 green, ModularityTests 2/2; 4b re-run clean apart from wording now aligned; 4c re-review found no confirmed issues and its two low plausibles were folded.
Decisions: Deeper refactors proposed by the review (moving the terminal set into SignatureStatus, deriving display status from stage, the view owning the first read) were not folded: they cross into untouched code or contradict the spec's stated rules; the backend query shape is registered as progress-read-hot-path. npm run build remains blocked by the pre-existing vitest advisory (frontend-dev-dep-refresh).
Halts: none
Follow-up CRs: `claim-bound-to-initiator` — first-claimant-wins on the emailed link, now in the register; `progress-read-hot-path` — polled read cost, now in the register
Modified files:
frontend/src/views/AgreementStatus.vue
frontend/src/views/AgreementStatus.test.ts
frontend/src/views/linkCopy.ts
frontend/src/composables/usePolling.ts
frontend/src/composables/usePolling.test.ts
frontend/src/api/signingProgress.ts
frontend/src/api/signingProgress.test.ts
frontend/src/App.vue
backend/src/test/java/in/agreementmitra/signing/signingrequest/SigningProgressApiIntegrationTest.java
backend/src/test/java/in/agreementmitra/signing/FulfilmentStageTest.java
docs/ROADMAP.md
openspec/changes/agreement-status-link-page/design.md
openspec/changes/agreement-status-link-page/tasks.md
openspec/changes/agreement-status-link-page/specs/agreement-status-view/spec.md

## manual-test — 2026-09-11T09:06:42.049Z  [⛔]
Outcome: Everything automated is green and the artifacts match the code; the flow now waits for a human to try the page for real before it can be archived.
Decisions: none
Halts: Recipe: run the stack (docker compose up -d; backend ./start_local.sh; frontend npm run dev). 1) Draft, finalise and pay an agreement anonymously (or waive via staff); open the emailed /agreement/<uuid> link: expect reference, property/rent/term, party names with roles and no contacts, timeline Drafted+Paid done and E-stamped current, no edit anywhere; reload and bookmark -> same page, URL unchanged. 2) As staff upload the stamp: without reloading, within ~20 s the page should move to E-stamped done and the first party's signature current. 3) Open the link for a claimed agreement anonymously: the 'This link no longer opens the agreement...' message with a Sign in button; signed in as a non-owner: same message, no button. 4) On a signed agreement: 'being prepared' until the PDF is stored, then Download works both anonymously (unowned) and signed in as owner (claimed). 5) Sanity: an unpaid-with-order agreement shows Complete payment; a finalised-but-unpaid page polls (Network tab: progress + payment every 20 s), a paid one polls progress only. Report anything off and it goes through the Issue policy before archive.
Follow-up CRs: none
Modified files:
openspec/changes/agreement-status-link-page/.flow-journal.md

## manual-test — 2026-09-11T09:49:07.465Z  [✅]
Outcome: The user clicked through the locally reachable steps of the recipe -- the link landing (terms, reference, party names only, timeline, no edit, reload keeps the URL), the stamp upload moving the page without a reload, the claimed-link message with and without a session, and the payment/polling sanity checks -- and reported them working. Step 4 (signed document after both parties sign) cannot be driven locally because the eSign vendor's callback needs a public host; it is accepted on integration evidence instead: ZoopSigningIntegrationTest drives init -> webhook -> fetch to SIGNED with artifacts stored, and SigningProgressApiIntegrationTest asserts the per-party statuses and the new stage/terminal/signedDocumentReady values after that same webhook, with the page's reaction to those values component-tested.
Decisions: Accepting integration evidence for the callback-dependent step was the user's call, recorded here rather than as a blanket pass; the real-vendor callback on a public host is tracked by the zoop-callback-e2e-on-public-host register row raised when zoop-aadhaar-esign archived.
Halts: none
Follow-up CRs: none
Modified files:
openspec/changes/agreement-status-link-page/.flow-journal.md

## archive — 2026-09-11T09:51:50.233Z  [✅]
Outcome: Archived via the openspec CLI after zoop-aadhaar-esign (which ADDED the progress requirement this change modified). Fold: agreement-status-view created with 7 requirements; signing-request 1 modified. Directory stamped 2026-09-11-095136Z. Task 3.2 was ticked in the archived tasks.md after the user's confirmation, with step 4 accepted on integration evidence.
Decisions: Single commit for code plus both archives (user chose A); the index already held this change's code, staged outside this session.
Halts: none
Follow-up CRs: none
Modified files:
openspec/specs/agreement-status-view/spec.md
openspec/specs/signing-request/spec.md
openspec/changes/archive/2026-09-11-095136Z-agreement-status-link-page/tasks.md
