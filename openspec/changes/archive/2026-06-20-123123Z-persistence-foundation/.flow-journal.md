# Flow journal — persistence-foundation

## propose — 2026-06-20  [✅]
Outcome: Drafted the full proposal set for a Flyway database-migration foundation — the agreed
prerequisite before the Aadhaar+OTP signing flow. It wires Flyway, sets the migration convention,
adds an infra-only baseline, and switches the test harness from `create-drop` to Flyway-managed
schema, so every later feature ships migrations on a solid, consistent base.
Decisions:
- Skipped Explore — requirement was concrete and bounded.
- Scope held tight (no split needed): pure infra, no feature schema/entities. Fits one-concern CR.
- New capability `backend-persistence`; MODIFIED delta on `backend-test-harness` (its test profile
  required `ddl-auto: create-drop` — now Flyway-managed).
- Baseline V1 = `CREATE EXTENSION pgcrypto` (infra-only, idempotent) over a comment-only no-op or
  `uuid-ossp` (rationale in design D3).
- `ddl-auto: validate` retained in all envs; `application-local.yml` `update` override removed.
- Unit test exempted (declarative config, tautological to mock — CR-5 precedent); integration test
  against Testcontainers Postgres is authoritative.
Halts: none.
Follow-up CRs: none new (these were already on the roadmap: minimal agreement domain →
create-signing-request tracer bullet → webhook+HMAC → real PDF rendering; ci-pipeline = CR-7).
Modified files: openspec/changes/persistence-foundation/{proposal,design,tasks}.md,
specs/backend-persistence/spec.md, specs/backend-test-harness/spec.md (all new).

## review — 2026-06-20  [✅]
Outcome: Ran the 3-persona review (architect/backend/security). No Criticals in the
breakage/secrets sense; findings clustered on the `pgcrypto` baseline and a missing destructive-op
safety. Folded every finding in-slice (no scope expansion, no signing-flow touch → no user halt),
then re-validated clean. Did not loop a 2nd review round — round 1 surfaced only minor/folded items
with clear oracles.
Decisions (review-driven):
- REVERSED the baseline: `pgcrypto` extension → **comment-only V1**. Kills C1 (test runs
  `postgres:16-alpine` not the standard image the design cited), m1 (gen_random_uuid is core PG13+),
  m5 (crypto surface widening). Comment-only still records V1 in flyway_schema_history.
- Added explicit `spring.flyway.clean-disabled: true` (M1) — make Boot's safe default intentional for
  audit-bearing data; new spec scenario + task + design D3a.
- Tightened test acceptance (M5): assert flyway_schema_history V1 (version=1, success=true) as the
  PRIMARY check (no more "and/or pgcrypto").
- Added lockfile verify task for flyway-database-postgresql BOM management (M2).
- Marked "validate-green-with-zero-entities" requirement TRANSITIONAL (M3/M4); noted in design D7
  that Flyway→JPA ordering + validate are first truly exercised by the first entity-bearing CR.
- Noted Boot default flyway.locations (m4), harness teardown behavior change (m3), and vestigial
  application-local.yml (m2) for apply-time.
Pre-existing observation (NOT this CR's scope): test PG image (16-alpine) vs compose (17) version
skew — flag only; fix belongs elsewhere.
Halts: none (all findings minimal/folded).
Follow-up CRs: none.
Modified files: design.md, tasks.md, specs/backend-persistence/spec.md (review revisions).

## apply — 2026-06-20  [✅]
Outcome: User reviewed artifacts then said proceed. Implemented all 6 task groups. Flyway 11.7.2
(BOM-managed, both artifacts, matching versions) wired; comment-only V1 baseline; clean-disabled +
validate posture in app/test/local config; test harness switched off create-drop. Full
`./run-tests.sh check` BUILD SUCCESSFUL; the Flyway integration test (2/2) asserts
flyway_schema_history has V1 (success); ModularityTests 2/2; OSV clean (159 pkgs); securityScan 0.
Bonus: live `bootRun` against compose Postgres 17 showed Flyway applied "v1 - baseline" cleanly
(verified the row directly in the DB) — the only stop was an unrelated pre-existing process holding
port 8090 (PID 90710), not our change.
Decisions: kept the redundant `spring.flyway.enabled: true` in application-local.yml as a non-empty
documented anchor (the file is otherwise vestigial after dropping ddl-auto: update).
Halts: none.
Follow-up CRs: none.
Modified files: backend/build.gradle.kts, backend/gradle.lockfile,
backend/src/main/resources/{application.yml, application-local.yml},
backend/src/main/resources/db/migration/V1__baseline.sql,
backend/src/test/resources/application-test.yml,
backend/src/test/java/in/agreementmitra/FlywayMigrationIntegrationTest.java, CLAUDE.md.

## validate — 2026-06-20  [✅]
Outcome: Read-only validate subagent cross-checked the diff vs all artifacts. `openspec validate
--strict` passes; every claim verified file:line; 0 code gaps; scope Non-Goals all honored. Lone
blemish: stale "(pgcrypto extension)" in design.md's own Migration Plan + a pgcrypto risk bullet —
doc-only, superseded by D3.
Decisions: none. Halts: none. Follow-up CRs: none.

## fix — 2026-06-20  [✅]
Outcome: Corrected the two stale pgcrypto mentions in design.md (Migration Plan step 2 → "comment-
only anchor"; the pgcrypto-availability risk bullet → comment-only no-op note). Re-validated clean.
Remaining pgcrypto refs are intentional D3 decision-history. Halts: none.
Modified files: design.md.

## manual-test — 2026-06-20  [⛔]
Outcome: Implementation done + validate clean; halting at the mandatory manual-test gate for user
confirmation before archive.
Halts: Asked the user to manually verify Flyway-managed schema. Proposed recipe — (1) `./run-tests.sh
check` from backend/ → BUILD SUCCESSFUL incl. FlywayMigrationIntegrationTest (already green here);
(2) optional live: `docker compose up -d postgres` then `SPRING_PROFILES_ACTIVE=local ./gradlew
bootRun` on a FREE port (8090 was occupied by a stray process PID 90710 during apply) → logs show
"Migrating schema public to version 1 - baseline" / "Successfully applied 1 migration", context
boots under validate; (3) `docker compose exec -T postgres psql -U agreementmitra -d agreementmitra
-c "select * from flyway_schema_history;"` → one row version=1, success=t. Awaiting user OK to
archive.
