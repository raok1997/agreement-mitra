# Flow journal — frontend-test-harness

## propose — 2026-06-20  [✅]
Outcome: Drafted the full proposal set (proposal, design, specs, tasks) for CR-3 of the
best-practices roadmap — a frontend Vitest + Vue Test Utils + jsdom test harness, a
repaired ESLint setup, a wired `npm test`, and two seed tests (api/client unit test +
SignDemo component test). `openspec validate --strict` passes.
Decisions: Scoped to the unit/component base of the pyramid only — E2E/Playwright and
cross-stack contract tests explicitly out (network is mocked). Config preference: extend
vite.config.ts `test` block over standalone vitest.config.ts. Co-located `*.test.ts`.
Mock the boundary (../api/client) in the component test, mock `fetch` in the client unit
test. Test-task exemption recorded (pure harness change; seed tests are the deliverable).
Halts: none — requirement was concrete and bounded, Explore skipped.
Follow-up CRs: contract-test/MSW (frontend↔backend SignSession shape) — noted as future
candidate, not in this CR. Frontend coverage-gate (vitest --coverage threshold) — deferred.
Modified files: n/a (artifacts only — proposal.md, design.md, specs/, tasks.md)

## review — 2026-06-20  [✅]
Outcome: Ran a 3-persona spec review (architect / senior frontend eng / security). One
real blocker found and fixed; several minor refinements folded in. Re-validates clean.
Decisions: All findings were minimal + in-slice → folded into artifacts (no scope
expansion, nothing signing-adjacent → no halt). Round 2 judged converged (every finding
had a clear oracle and a verifiable fix); skipped a second full review agent since
SendMessage to continue the round-1 agent wasn't available and a cold re-review would
just re-derive context.
Halts: none.
Findings folded:
  - MAJOR (architect): co-located *.test.ts would break `vue-tsc -b` (tsconfig include
    src/** + noUnusedLocals). Fix: add `exclude: ["src/**/*.test.ts"]` to prod tsconfig +
    separate tsconfig.vitest.json for vitest/globals types (task 1.3, design decision,
    verify task 5.3).
  - MAJOR (senior eng): loading-state test must use a deferred unresolved promise, NOT
    mockResolvedValue, or it asserts the wrong frame. Spelled out in task 4.3 + design Risks.
  - MINOR: flat-config .vue parsing needs vue-eslint-parser + parserOptions.parser:
    tseslint.parser (task 2.2, design). "Clean sources pass lint" is a build-time gate —
    fix flagged source, no blanket disables (task 2.4).
  - MINOR: pin Vitest to 3.x (Vite 6 peer) — task 1.1, design.
  - MINOR (security): future dep-vuln scan must cover devDependencies (proposal Impact);
    commit the updated package-lock.json (task 5.4).
  - Suggestion: assert the EDITED agreement id, not the default (task 4.2).
Follow-up CRs: none new (contract-test/MSW + coverage-gate already noted in propose).
Modified files: proposal.md, design.md, tasks.md (artifacts only)

## apply — 2026-06-20  [✅]
Outcome: Implemented the full harness. All 18 tasks done; `npm test` (7 tests, 2 files)
passes, `npm run lint` clean (0 problems), `npm run build` passes. Working tree clean of
build artifacts.
Decisions: ESLint flat config composes @eslint/js + typescript-eslint + eslint-plugin-vue
flat/recommended, with a .vue block wiring vue-eslint-parser→tseslint.parser. Disabled 3
eslint-plugin-vue *formatting* rules (singleline-content-newline, max-attributes-per-line,
html-self-closing) with rationale: Prettier owns formatting, so they conflict — not blanket
disables. Vitest config lives in vite.config.ts `test` block. Added @eslint/js +
vue-eslint-parser to devDeps (needed by the flat config).
In-scope hygiene fix (pre-existing, fixed since this CR owns the build config): `vue-tsc -b`
was emitting stray .js next to sources + a tsbuildinfo (tsconfig had no noEmit). Added
`"noEmit": true` to tsconfig.json (build still green, JS pollution gone) and `*.tsbuildinfo`
to frontend/.gitignore. Cleaned the stray emits from the tree.
Halts: none — no signing/eSign code touched, no module boundaries, pure frontend tooling.
Modified files: frontend/package.json, frontend/package-lock.json, frontend/vite.config.ts,
frontend/tsconfig.json, frontend/.gitignore (modified); frontend/eslint.config.js,
frontend/tsconfig.vitest.json, frontend/src/api/client.test.ts,
frontend/src/views/SignDemo.test.ts (new); openspec/changes/frontend-test-harness/tasks.md
(all 18 checked).

## validate — 2026-06-20  [✅]
Outcome: Isolated subagent cross-checked all 12 spec scenarios against the two test files'
actual assertions, plus design decisions and scope exclusions vs. the working tree. 0
blocking, 0 should-fix gaps. Implementation matches spec.
Decisions: none — no fixes required, Fix stage is a no-op.
Halts: none.
Note (non-blocking): "Clean sources pass lint" is satisfied by disabling 3 eslint-plugin-vue
*stylistic* rules (Prettier owns formatting) rather than editing source — within the design's
explicit allowance; targeted per-rule disables with rationale, not blanket.
Modified files: none (read-only validation).

## archive — 2026-06-20  [✅]
Outcome: Manual-test gate passed (user confirmed npm test/lint/build). Synced the new
`frontend-test-harness` capability to openspec/specs/frontend-test-harness/spec.md (new
capability — ADDED requirements became the main spec) and archived the change.
Decisions: none.
Halts: none.
Modified files: openspec/specs/frontend-test-harness/spec.md (new main spec); change dir
moved to openspec/changes/archive/2026-06-20-031727Z-frontend-test-harness/.
