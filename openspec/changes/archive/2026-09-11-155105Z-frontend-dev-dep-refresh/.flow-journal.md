# Flow journal — frontend-dev-dep-refresh

## propose — 2026-09-11T10:07:08.467Z  [✅]
Outcome: Drafted the full artifact set for unblocking the repo-wide frontend build gate: bump vitest/@vitest/mocker 3.2.7 to 4.1.11 to clear GHSA-82fw-gwwq-j7x9, and give frontend/scripts/*.mjs Node globals in the eslint config to clear 2 no-undef errors. openspec validate --strict passes.
Decisions: Upgrade rather than suppress (a suppression's honest reason would be reluctance, and the gate is fail-on-any). Verified BEFORE proposing that vitest 4.1.11 peers vite ^6||^7||^8 and node ^20||^22||>=24, both already satisfied — so no Vite/Node bump rides along and no CR split is needed. Fix the lint error with an inline scoped languageOptions.globals block rather than taking the `globals` package direct, because this CR exists to shrink the dependency-scan surface. Recorded a green pre-bump baseline (27 files / 230 tests / 2.72s) so any post-bump failure is attributable. One spec delta: MODIFY 'Working ESLint lint script' in frontend-test-harness to bring frontend/scripts/ into the linted scope with Node globals, and to state the pass bar as zero ERRORS (warnings are advisory). frontend-security-scanning gets no delta — clearing a finding by upgrading is conformance with its fail-on-any requirement, not a change to it. Test-task exemption claimed (pure tooling/config), noted atop tasks.md.
Halts: None yet. One scope call deferred to the review stage summary: the 26 surviving vue/html-indent + vue/html-closing-bracket-newline WARNINGS are declared out of scope and are queued as a new follow-up-register row (tasks 5.2), not folded in.
Follow-up CRs: none

## review — 2026-09-11T10:16:02.906Z  [⛔]
Outcome: Round 1 of the three-persona review (Architect, Senior Engineer, Security — all three returned) found 1 Critical and 8 Major issues. The artifacts are directionally right but the vite-node remedy I added mid-flight is unproposed, unversioned and not executable as written, and the lint fix does not deliver the property its spec delta claims. Halting for a design decision before any code is written.
Decisions: Coverage matrix built and written into tasks.md: 3 scenarios, 3 COVERED, 0 UNMAPPED, 0 WAIVED — but the review showed the matrix is VACUOUS, since all three rows map to `npm run lint` exiting 0, which `ignores: ["scripts/"]` or `no-undef: off` would satisfy equally. Needs a discriminating oracle before it means anything. openspec validate --strict passes (structural check clean).
Halts: CRITICAL — task 4.0 (declare vite-node so `npm run terms:doc` survives the bump) appears in no proposal/design/Impact section, contradicts proposal.md's explicit 'No new devDependency', names no version, and the obvious `npm i -D vite-node` resolves 6.0.0 which hard-depends on vite ^8 — measured at +32 packages including a nested second Vite major. The only vite ^6-compatible line is vite-node@3.2.4, the terminal release of a branch vitest 4 abandoned; under a fail-on-any gate the first advisory against it forces either a suppression or the vite major this CR exists to avoid. Four alternatives (vite-native ssrLoadModule, npx on demand, node type-stripping, retire the script) were never considered. Presented options to the user; awaiting the scope/design call before the fix round.
Follow-up CRs: none new yet — the deferred .vue lint warnings are already task 5.2, to be added to the register at that point

## review (round 2) — 2026-09-11T10:36:43.221Z  [✅]
Outcome: Round 2 found 2 Criticals and 8 Majors — notably that three of round 1's nine fixes bred new defects. All are now addressed in the artifacts, and the two core mechanisms were proven empirically against the live repo before being written down.
Decisions: Fixed, each verified by measurement rather than argument: (1) the ESLint globals spread order is now explicitly browser-off-FIRST then globals.node — the sets overlap on 60 names, so the order I originally wrote would have stripped `console` and traded two `process` errors for two `console` errors; measured 0 errors / 26 warnings with the correct order. (2) Added `await server.close()` in a finally — the generator as previously specified produced correct bytes and then hung forever. (3) Added `server: { ws: false }` — the default binds an unauthenticated HMR WebSocket on :24678 across ALL interfaces, which in a CR closing an unauthenticated-HMR-socket advisory is indefensible; measured 1 listener by default, 0 with the flag, identical 15216-byte output. (4) Removed `fetch` from the spec's browser-exclusion list and rewrote it as 'browser-exclusive (703 = browser minus node)' — as written it was unsatisfiable. (5) Scoped the spec's no-undef clauses to .js/.mjs/.cjs, since typescript-eslint disables no-undef for .ts, so a .ts clause would have been silently violated by the shipped config. (6) Moved the `globals` declaration into 2.1 so the single install covers it — previously package.json and the lockfile would have diverged and npm ci would fail. (7) Replaced the inert vue-tsc mitigation with `vue-tsc -p tsconfig.vitest.json --noEmit`; the old one could never fire because tsconfig.json has no references and excludes test files. (8) Corrected the mockReset reading list — CaptureForm (22) and App (11) are 33 of 47 sites, while the two files I had named contain zero. (9) Dropped `configFile: false` in favour of the real vite.config.ts so the generator resolves modules as the test does; verified byte-identical either way. (10) Corrected the install-script claim ('esbuild + fsevents' was wrong in four places — only esbuild has one) and the `npm audit signatures` rationale, which does not detect compromised-maintainer publishes.
Halts: None — round 2's findings were all in-slice corrections to artifacts, folded under Issue policy level 1/2. Two new follow-up register rows were identified and added as tasks 5.4 (terms:doc is gated by nothing — this CR's own root-cause finding) and 5.5 (engines.node vs vitest-4 matrix mismatch).
Follow-up CRs: `frontend-vue-lint-warnings`, `frontend-config-driven-test-teardown`, `terms-doc-ungated`, `frontend-engines-node-narrowing` — all to be added to the register at tasks 5.2-5.5

## review (round 3) — 2026-09-11T14:49:08.295Z  [⚠️]
Outcome: Round 3 was a focused verification of round 2's fixes. Both subagents died on a session rate limit before producing any findings, so I ran the checks in the main thread instead. Three defects found and fixed; the substantive round-2 fixes verified sound.
Decisions: Verified in the spine rather than re-spawning agents: (1) `vue-tsc -p tsconfig.vitest.json --noEmit` exits 0 today, so task 2.8's new oracle is valid and has a clean pre-bump baseline — it was worth checking, since the mitigation it replaced was inert. (2) Coverage matrix re-checked mechanically against the delta: 6 scenarios, 6 rows, same order, every row naming a task that exists. (3) The `--ignore-scripts`/re-install sequence holds, but for a different reason than the task implied — esbuild's binary ships in the @esbuild/<platform> package (a normal dep needing no script) and generateBinPath() resolves it at runtime, while a plain re-install is a NO-OP if nothing changed. Rewrote task 2.4 to verify rather than assume, and recorded why it passes either way. (4) Fixed a stale cross-reference: design.md said 'task 2.5 asserts clean stderr' after renumbering moved it to 2.6. (5) Swept for the retracted 'esbuild + fsevents' claim — both surviving mentions are the corrected form.
Halts: None. Review closes here at 3 rounds (cap is 5): round 3 found only a stale cross-reference and one task that assumed where it should verify — no Criticals, no Majors, and no new defects bred by round 2's fixes, which breaks the round-1/round-2 pattern. Marked warn rather than ok because the round-3 subagents never ran; the verification is mine, and is narrower than a full three-persona pass.
Follow-up CRs: none
Modified files:
openspec/changes/frontend-dev-dep-refresh/design.md
openspec/changes/frontend-dev-dep-refresh/tasks.md

## apply — 2026-09-11T14:49:29.258Z  [⛔]
Outcome: Task 1.1 — pre-change baseline recorded before any edit.
Decisions: Baseline measured with osv-scanner 2.4.0 / osv-scalibr 0.4.5 at 2026-09-11T14:49:21Z (the gate is local-only until CR-7, so the scanner build is part of the result). security:scan = 2 findings, GHSA-82fw-gwwq-j7x9 on vitest 3.2.7 and @vitest/mocker 3.2.7. lint = 28 problems: 2 errors ('process' is not defined at scripts/security-scan.mjs:37 and :41) + 26 warnings. test = 27 files / 230 tests green in 2.72s. terms:doc = green, docs/TERMS-OF-SERVICE.md 15216 bytes. vue-tsc -p tsconfig.vitest.json --noEmit = exit 0, no output.
Halts: Stage in progress — entry written at task 1.1 so the baseline survives a compaction. Will be superseded by the apply completion entry.
Follow-up CRs: none
Modified files:
openspec/changes/frontend-dev-dep-refresh/tasks.md

## apply — 2026-09-11T14:55:44.033Z  [✅]
Outcome: All 23 tasks implemented. The repo-wide frontend build gate is restored: `npm run build` now completes end to end (security:scan + 230 tests + vue-tsc + vite build) in ~6s, `npm run security:scan` reports 'No issues found', and `npm run lint` exits 0 with zero errors.
Decisions: Measured outcomes: dependency graph is net −7 packages (+obug/@standard-schema/spec/convert-source-map, −vite-node/tinypool/tinyspy/cac and six others), exactly as predicted. globals@14.0.0 deduped onto the existing @eslint/eslintrc copy — zero new lockfile nodes, as designed. Provenance gate passed: exactly one lifecycle install script in the tree (esbuild), all 322 registry signatures verified, no halt. Suite went 230/230 green under vitest 4.1.11 with NO test file edited, and stayed green across three shuffled runs. vue-tsc -p tsconfig.vitest.json --noEmit exits 0. The terms generator now runs on Vite's ssrLoadModule with ws:false / server.close() / root-from-import.meta.url, verified to exit 0 with empty stderr, byte-identical output, no listener on :24678, and working from the repo root. The lint fix was verified behaviourally as well as by config: a probe file showed `localStorage` raising no-undef while `process` does not, and src/main.ts still has its browser globals.
Halts: One anomaly investigated rather than waved through: the full suite prints a jsdom 'Not implemented: navigation' error, traced to CaptureForm.test.ts — the very file carrying the 22-bare-mockReset beforeEach that design.md named as the highest-risk pattern for vitest 4's mockReset semantics change. Rather than assume, I installed vitest 3.2.7 with --no-save and re-ran: the warning appears there too. It is PRE-EXISTING, not a regression from this CR, and all 230 tests pass either way. Left alone as out of scope.
Follow-up CRs: `terms-doc-ungated`, `frontend-config-driven-test-teardown`, `frontend-engines-node-narrowing`, `frontend-vue-lint-warnings` — all four added to the docs/ROADMAP.md register; the closed `frontend-dev-dep-refresh` row removed from it.
Modified files:
frontend/package.json
frontend/package-lock.json
frontend/eslint.config.js
frontend/scripts/render-terms.mjs
frontend/src/content/termsDocPath.ts
docs/ROADMAP.md
openspec/changes/frontend-dev-dep-refresh/tasks.md

## validate — 2026-09-11T15:09:32.849Z  [✅]
Outcome: All three Stage-4 gates clean. 4a: `npm run build` completes end to end in ~5.9s wall clock (No issues found + 230/230 + vite build). 4b: spec-conformance validation returned ZERO gaps across 23 ticked tasks, with all 6 coverage rows independently re-executed green. 4c: code review returned three findings, none critical.
Decisions: 4b confirmed every non-goal actually held — the 26 lint warnings unfixed, the 47 .mockReset() calls untouched, vite/eslint/typescript unchanged, no CI added, osv-scanner.toml unmodified with zero live ignore entries. 4c dispositions under the Issue policy: FOLDED (1) the `finally { await server.close() }` error-masking bug — a rejecting close would replace the original ssrLoadModule error and hide the real cause, so it is now wrapped in its own try/catch; (2) five globals (navigator/Navigator/WebSocket/Performance/CloseEvent) that globals@14 calls browser-only but which are real on globalThis in modern Node, re-added as readonly so a tooling script using them does not get a false no-undef. NOT FOLDED: extending the scoped block to the four root-level Node configs (eslint.config.js, vite.config.ts, postcss.config.js, tailwind.config.js), which keep the inverted globals — that widens the spec's stated scope beyond frontend/scripts/ and is pre-existing rather than a regression, so it became register row `frontend-root-config-node-globals`. Round-2 security had raised the same boundary question and I had drawn the line deliberately but never registered it; that omission is now closed.
Halts: None. Re-verified after the folds: lint exit 0, terms:doc exit 0 with empty stderr and unchanged output, the failure path still exits 1, the print-config assertions all pass including the five re-added globals, and the full build is green.
Follow-up CRs: `frontend-root-config-node-globals` — added to the docs/ROADMAP.md register
Modified files:
frontend/scripts/render-terms.mjs
frontend/eslint.config.js
docs/ROADMAP.md
openspec/changes/frontend-dev-dep-refresh/design.md

## archive — 2026-09-11T15:51:30.690Z  [✅]
Outcome: Archived via the openspec CLI as 2026-09-11-155105Z-frontend-dev-dep-refresh. The delta folded into the spec of record cleanly: frontend-test-harness updated, totals + 0, ~ 1, - 0, → 0.
Decisions: Pre-archive checks passed before the CLI ran: followups gate clean (3 recorded follow-ups, all naming slugs already in the register — 42 slugs held), only one capability touched, no sibling active change holds a frontend-test-harness delta, and the MODIFIED target 'Working ESLint lint script' existed in the baseline at spec.md:40 so the rebuild could not abort. Reviewed the fold diff itself (70 insertions / 8 deletions): the one modified requirement plus its five scenarios, with the other four requirements in the capability untouched; the only incidental changes are two blank lines and an EOF newline from CLI normalisation. Proceeded on the user's explicit instruction without a reported manual-test result — flagged at the time rather than treated as a pass.
Halts: None.
Follow-up CRs: none
Modified files:
openspec/specs/frontend-test-harness/spec.md
openspec/changes/archive/2026-09-11-155105Z-frontend-dev-dep-refresh/
