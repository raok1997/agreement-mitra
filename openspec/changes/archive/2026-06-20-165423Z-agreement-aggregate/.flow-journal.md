# Flow journal — agreement-aggregate (CR-2, signing-flow roadmap, feat/signing-flow-v2)

## explore — 2026-06-20  [✅ skipped]
Outcome: Requirement was concrete and bounded from a prior in-thread discussion; no explore stage needed.
Decisions: Three design forks resolved with the user before proposing — (1) signers as child @Entity (not @ElementCollection); (2) carry the reference field set (rent/deposit/term + property address); (3) strict validation (≥1 owner, ≥1 tenant, unique emails). The discarded singular-role version lives only on feat/best-practices (unmerged), so this branch is a clean slate.
Halts: none
Follow-up CRs: none

## propose — 2026-06-20  [✅]
Outcome: Drafted the full proposal set for a status-less, multi-party Agreement aggregate (N owners + N tenants as addressable child signers) with create/read endpoints, strict create-time validation, a V2 Flyway migration, and a sandbox security permit. openspec validate --strict passes.
Decisions: New capability `agreement-management` (no existing spec modified — signing FSM/webhook/eSign are unspecified today and owned by later CRs). Cross-field rules (role mix + email uniqueness) chosen as a class-level bean-validation constraint so they're unit-testable without Spring (design D4). Scope check: one aggregate + its create/read pair = within a single CR slice, no split.
Halts: none
Follow-up CRs: none
Modified files: openspec/changes/agreement-aggregate/{proposal.md, design.md, specs/agreement-management/spec.md, tasks.md, .flow-journal.md}

## review — 2026-06-20  [⛔ halt: confirm scope split before apply]
Outcome: Two-round multi-persona review. Round 1 surfaced 1 Critical, 8 Major, 9 Minor, 4 Suggestions — all within CR-2's own slice (no other aggregate/endpoint/FSM, nothing signing-flow-adjacent), so all foldable per Issue policy. Folded them into the four artifacts; round-2 confirmation review found one residual rent-validation inconsistency (zero-rent allowed in 2 spots vs strictly-positive intended), now fixed across design D8 + spec. openspec validate --strict green. Converged in 2 rounds (cap 5).
Decisions: Critical fix — AgreementService is Java-public but Modulith-internal (package-private class can't be injected by signing.api controller). Pinned JPA mapping bidirectional @OneToMany + Persistable isNew + @Transactional under open-in-view:false. Scoped security to POST /api/agreements + GET /api/agreements/* (not /** wildcard, preserving fail-closed posture). Added rental-term/money bounds (rent strictly positive, deposit >=0, @Digits 10.2, term positive), signer cap 1-20, case-insensitive email dedup, id-only toString for PII hygiene under DEBUG logging.
Halts: Halted before Apply to confirm (a) converged spec good to implement, and (b) scope split for finding #8. RESOLVED 2026-06-20 — user chose "implement, defer error-handler CR": accept opaque 400 bodies in CR-2, spin validation-error-responses as a separate follow-up CR.
Follow-up CRs: validation-error-responses — global @RestControllerAdvice → ProblemDetail surfacing field-level validation errors safely (no input echo); deferred from CR-2 finding #8.


## apply — 2026-06-20  [done]
Outcome: Implemented the full Agreement aggregate — V2 Flyway migration, Role/Signer/Agreement domain + repository + public service, the api records + @ValidSignerSet constraint + AgreementController, and two scoped SecurityConfig permits. Added 22 tests. Full ./gradlew check green via run-tests.sh: 36/36 tests pass, 0 skipped (integration ran against Testcontainers Postgres — Flyway V2 applied + schema validated under ddl-auto:validate), OSV 0 issues, SpotBugs/Spotless/JaCoCo/ModularityTests green.
Decisions: Two visibility deviations from the written tasks, both forced by Java + documented in code: (1) Role enum is public, not package-private (the signing.api records must name it; still Modulith-internal). (2) SignerSetValidator is public, not package-private — Bean Validation's default factory (no-Spring unit-test path) needs a public no-arg constructor; package-private threw HV000064 standalone while Spring's factory had tolerated it. Service<->api is an intra-module bidirectional package reference (fine within one Modulith module).
Halts: none (self-corrected one misdiagnosis: first read the unit-test failures as an EL issue, real cause was the package-private validator constructor).
Follow-up CRs: validation-error-responses (deferred from review #8).

## validate — 2026-06-20  [✅]
Outcome: Read-only cross-check of the implemented diff vs proposal/design/spec/tasks (isolated subagent). Every spec requirement + scenario COVERED with file:line evidence; all 11 design decisions (D1–D11) honored; all 4 journaled deviations confirmed. No must-fix gaps.
Decisions: none
Halts: none
Follow-up CRs: validation-error-responses (unchanged)

## fix — 2026-06-20  [✅]
Outcome: Closed the one nice-to-have validate finding — the spec scenario "client-supplied ids and timestamp are ignored" had no direct test (was enforced only structurally). Added integration test clientSuppliedIdAndCreatedAtAreIgnored (POSTs a body with id+createdAt, asserts the server assigns its own). Full ./gradlew check green: 37/37 tests, 0 skipped; OSV/SpotBugs/Spotless/JaCoCo/ModularityTests green. The other nice-to-have (ModularityTests assertion) needs no action — it runs in the suite.
Decisions: Folded the gap per Issue policy (minimal, in-scope, test-only — no code/artifact change).
Halts: none
Follow-up CRs: validation-error-responses (unchanged)

## manual-test — 2026-06-20  [⛔ awaiting user confirmation]
Outcome: Implementation complete + validate clean (37/37 tests green incl. full integration against Testcontainers Postgres). Halting for user manual test before archive (archive is never automatic).
Decisions: none
Halts: Handed off a manual-test recipe — docker compose up -d (Postgres+MinIO), ./gradlew bootRun (Flyway applies V1+V2), then curl POST/GET against /api/agreements covering 201 create (server ids + ISO-8601 createdAt), 200 round-trip, 404 unknown, 400 validation (no-tenant + duplicate-email leave no rows), and 403 on a denied sub-path (DELETE). Awaiting user "it works" to proceed to archive; a reported defect routes back through the Issue policy.
Follow-up CRs: validation-error-responses (unchanged)

## manual-test RESOLVED — 2026-06-20  [✅]
Outcome: User ran bootRun + curl recipe and confirmed "it works" (201/200/404/400/400/403). One snag during testing: local compose Postgres carried a stale "version 2" Flyway row + old `agreement` table from the discarded feat/best-practices V2 → checksum mismatch on bootRun. Fixed surgically (DROP stale agreement table + DELETE V2 history row in local DB; MinIO + V1 untouched) — NOT a code change; the V2 migration file was always correct. DB reset to V1 baseline; new V2 then applied clean. Cleared to archive.
Halts: none
