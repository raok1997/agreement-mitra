# Flow journal — create-signing-request (CR-3)

## propose (grounding) — 2026-06-21T06:00:43Z  [⛔]
Outcome: Mapped the existing signing module before drafting. Found that the requirement assumes "the agreement FSM" exists, but the Agreement aggregate is deliberately status-less (CR-2 decision) and SignatureStatus is an unused enum with no persisted home. Halting to confirm where signing state lives and the CR's scope before generating artifacts.
Decisions: RESOLVED — (1) NEW SigningRequest aggregate is the FSM home (own V3 table, references agreement, keeps Agreement status-less). (2) Keep as ONE CR-3 (create + webhook end-to-end tracer bullet). (3) Default accepted: multi-invitee SignRequest/SignSession; single /api/webhooks/esign endpoint branching on webhookType.
Halts: Two forks put to user —
  (1) FSM-state home: NEW SigningRequest aggregate (own table, keeps Agreement status-less, references agreement) [recommended] vs add status to Agreement (reverses CR-2).
  (2) Scope: keep as ONE tracer-bullet CR (create + webhook end-to-end) [recommended] vs split CR-3a create / CR-3b webhook.
  Also noted (will default, not blocking): SignRequest is single-signer and SignSession is single-URL today; Agreement is multi-party, Leegality body is invitees[] — recommend extending both DTOs to multi-invitee (per-invitee signUrl+expiryDate).
Follow-up CRs: none yet.

### Grounding map (key facts, from Explore)
- EsignProvider: `createSignRequest(SignRequest)->SignSession`, `getStatus(String)->SignatureStatus`, `download(String)->SignedDocument`, `verifyWebhook(String payload, String signatureHeader)->boolean`. CR-3: implement createSignRequest + getStatus (Details API = authoritative status); drop signatureHeader from verifyWebhook (mac is in body); `download` stays a stub.
- LeegalityEsignProvider @ signing.leegality — all 4 methods stub UnsupportedOperationException. Internal package (not exported).
- SignSession = record(providerRequestId, signingUrl) — SINGLE url; needs multi-invitee reshape.
- SignRequest = record(agreementId, signerName, signerEmail, unsignedPdf) — SINGLE signer; multi-party mismatch.
- SignatureStatus enum (DRAFT, PDF_GENERATED, SIGN_REQUESTED, SIGNED, FAILED, EXPIRED) — public API, NO persisted entity yet.
- Agreement aggregate @ signing.agreement — status-less (CR-2), package-private entity, factory create(...), addSigner(...). Signers: OWNER/TENANT (Role enum, public). Table `agreement`+`signer` (V2).
- Controllers @ signing.api: SigningController POST /api/signing/{agreementId}/request (stub UOE), WebhookController POST /api/webhooks/esign (verifyWebhook then 401/202, no FSM). AgreementController (CR-2, working).
- Errors: GlobalExceptionHandler @ root (RFC 9457 ProblemDetail, urn:agreementmitra:problem:*, never echoes PII). Webhook errors should follow it.
- Config: application.yml esign.leegality.{base-url,auth-token,webhook-secret} from env; NO profileId yet; NO HTTP client bean yet. Test/local profiles pin secrets empty.
- Tests: Testcontainers (PG+MinIO) harness; @ApplicationModuleTest slices; NO WireMock dependency yet (CR-3 needs it). ModularityTests guards .agreement/.leegality boundaries.
- Migrations: V1 baseline, V2 agreement+signer. No signing-request table → V3 needed if we persist signing state.
- SecurityConfig permits POST /api/webhooks/esign and POST /api/signing/*/request (fail-closed otherwise).

## propose — 2026-06-21T06:00:43Z  [✅]
Outcome: Generated full artifact set (proposal + design + specs + tasks) for the SigningRequest tracer bullet. New capability `signing-request`; Agreement stays status-less (no modified capabilities). `openspec validate` passes.
Decisions: New SigningRequest aggregate owns FSM (own V3 table + child invitee table); single /api/webhooks/esign branching on webhookType; multi-invitee SignRequest/SignSession; verifyWebhook drops signatureHeader; RestClient bean; WireMock test dep; webhook=trigger, Details API=source of truth (shared with future reconciliation).
Halts: none (scope/FSM-home forks already resolved at grounding halt).
Follow-up CRs: none yet (signed-PDF/audit-trail download + object storage explicitly deferred).
Modified files: proposal.md, design.md, specs/signing-request/spec.md, tasks.md (all new under the change dir).

## review — 2026-06-21T06:00:43Z  [⛔]
Outcome: Multi-persona review (architect + senior eng + security) returned 2 Critical, 8 Major, 9 Minor, 5 Suggestions. Strong report. Most findings are cheap spec/correctness/clarity folds; two are real scope decisions on signing-flow-adjacent code → halting for user.
Decisions: (pending user) — triage below.
Halts: Two scope calls put to user —
  (A) AUTH/ABUSE on the now-live, side-effecting create endpoint (unauthenticated PII egress to vendor + invite-spam + quota burn keyed on an agreement UUID). Defer real authZ+rate-limit to a dedicated CR (document risk now, rely on sandbox/dummy-only) [recommended] vs fold a minimal abuse guard now vs build ownership authZ now.
  (B) FOLD DEPTH for the correctness/spec items (orphan-on-DB-failure persist-first, optimistic locking/@Version, Details-API-failure webhook policy, replay scenario, constant-time-in-spec, oracle-proof 202, body-size limits, signer FK, status col type, stale SecurityConfig comment+test, getStatus non-terminal SENT, re-request rule, EsignProvider seam spec anchor). Fold all now [recommended] vs keep tracer thin and defer the heavier ones.
Follow-up CRs (candidate): signing-auth (ownership authZ + rate-limit + security-event logging); already-planned reconciliation CR absorbs Details-failure retries / missed hooks.

### Review triage (disposition I propose)
FOLD now (cheap correctness/clarity, no functional scope growth):
  - CRIT-1 orphaned-vendor-doc on DB-fail → persist intent BEFORE provider call, update documentId after; +scenario. (also fixes tx-boundary Major: don't hold tx across HTTP).
  - getStatus non-terminal (Leegality SENT) → no FSM transition / no-op; +scenario.
  - Optimistic locking (@Version) on SigningRequest for concurrent webhook delivery; +scenario.
  - Details-API failure on webhook → ACK (so vendor doesn't hammer) + leave to reconciliation; +scenario. (design decision)
  - Replay of a valid captured webhook → explicit scenario: harmless via idempotent+Details-driven.
  - Constant-time MAC compare → promote from task to spec requirement.
  - Unknown-documentId webhook → indistinguishable 202 (no existence oracle); tighten scenario.
  - Body-size/content-type limits on both endpoints (cheap DoS guard); +requirement+task.
  - signing_request_invitee → add signer_id FK (not email-only); D8/migration.
  - status column type pinned (varchar(32)) for deterministic ddl-auto:validate.
  - stale SecurityConfig "(deferred)" comment + SecurityBaselineIntegrationTest update → task.
  - unsigned-PDF bytes are SERVER-sourced (not a client field) → spec note (anti-mass-assignment).
  - re-request rule: allow multiple SigningRequests per agreement, each its own row → one-line spec note.
  - EsignProvider internal-API change anchored in provider-seam spec requirement.
  - non-blocking create + non-terminal-no-op test tasks.
DEFER (scope/production-hardening → follow-up CR, pending A):
  - Real ownership authZ on create; rate-limiting; security-event logging of verify-failures/enumeration.

## review (resolution) — 2026-06-21T06:00:43Z  [✅]
Outcome: Decisions taken: (A) defer authZ+rate-limit+security-event-logging to a follow-up `signing-auth` CR (documented accepted risk; sandbox/dummy-only; body-size limits added now); (B) fold all cheap correctness/clarity items. Applied folds across proposal/design/spec/tasks. Round-2 re-review confirmed every round-1 item ADDRESSED or DEFERRED-OK and found 3 new Minors — all folded:
  - NEW-1 content-type: webhook now consumes application/json.
  - NEW-2 (real contradiction): design listed document.status as only DRAFT/SENT/COMPLETED but mapping needs reject/cert-fail/expire → broadened design to "authoritative lifecycle incl. terminal non-success outcomes, exact tokens pinned vs sandbox".
  - NEW-3 verifyWebhook contract: now returns the verified documentId (Optional<String>) so the controller stays vendor-neutral.
Decisions: New folds — persist-before-provider (D9), @Version optimistic locking (D10), Details-API-failure ack+reconcile (D11), replay/oracle/constant-time hardening (D12), authZ deferred (D13), signer_id FK + bounded status type + unique documentId (D8). verifyWebhook→Optional<String> documentId (D5).
Halts: resolved (auth scope + fold depth answered by user). Review converged in 2 rounds (cap 5).
Follow-up CRs: signing-auth (ownership authZ + rate-limit + redacted security-event logging); reconciliation job (reuses verify→Details→FSM, recovers interrupted pre-request rows); signed-document download + object storage.
Modified files: proposal.md, design.md, specs/signing-request/spec.md, tasks.md.

## review — 2026-06-21T06:00:43Z  [✅]
Outcome: Spec package reviewed (2 rounds) and hardened; openspec validate clean. Ready to implement.
Decisions: see resolution block above.
Halts: none outstanding.
Follow-up CRs: signing-auth; reconciliation; signed-document download (all out of scope here).

## apply — 2026-06-21T06:00:43Z  [✅]
Outcome: Implemented all 8 task groups end-to-end. Full gate green via ./run-tests.sh: 91 tests, 0 failures, 0 skipped (Docker up → integration ran); ModularityTests, JaCoCo, OSV scan, SpotBugs/FindSecBugs all pass.
Decisions during apply:
  - Pre-request FSM state = PDF_GENERATED (distinct from SIGN_REQUESTED), so FSM is PDF_GENERATED→SIGN_REQUESTED→SIGNED|FAILED|EXPIRED. D9 persist-first uses two short txs (SigningRequestPersistence) around the provider call (SigningRequestService is NOT @Transactional).
  - signer_id stored as a plain UUID column + DB FK (not a JPA association) to avoid the .signingrequest package reaching into .agreement entities — keeps ModularityTests green.
  - Forced HTTP/1.1 on the Leegality RestClient (JdkClientHttpRequestFactory): JDK client's h2c upgrade caused intermittent RST_STREAM against WireMock (and the real API speaks 1.1). This was the root cause of initial flaky 500s.
  - Body-size guard = a servlet OncePerRequestFilter (413 on Content-Length > 1 MiB) over /api/webhooks/esign + /api/signing/*.
  - verifyWebhook returns Optional<String> documentId; getStatus maps non-terminal/unknown → SIGN_REQUESTED (safe no-op).
Halts: none (all failures were trivially fixable: HTTP/1.1 pin + updating SecurityBaselineIntegrationTest's stale 500-posture assertions to 401/400 per task 5.3).
Follow-up CRs: unchanged (signing-auth; reconciliation; signed-document download).
Modified files (main): EsignProvider, SignRequest, SignSession, leegality/{LeegalityEsignProvider,LeegalityConfig,LeegalityProperties}, signingrequest/{SigningRequest,SigningRequestInvitee,SigningRequestRepository,SigningRequestPersistence,SigningRequestService}, api/{SigningController,WebhookController,SigningRequestResponse,PayloadSizeLimitConfig}, SecurityConfig, application.yml, db/migration/V3__signing_request.sql, build.gradle.kts, gradle.lockfile.
Modified files (test): application-test.yml, SecurityBaselineIntegrationTest (updated), + new: SigningRequestStateMachineTest, SigningRequestServiceTest, leegality/{LeegalityWebhookVerificationTest,LeegalityMappingTest,LeegalityEsignProviderWireMockTest}, SigningRequestApiIntegrationTest.

## validate — 2026-06-21T06:00:43Z  [✅]
Outcome: Ran openspec-validate in an isolated subagent (read-only). Structural `openspec validate` passes. Code cross-checked against all artifacts: ~30 requirements/decisions verified satisfied. ONE minor artifact/code mismatch: artifacts promised "branch on webhookType" but code (correctly) doesn't — Details-API-as-truth makes branching on the untrusted, non-MAC-covered body field unnecessary/wrong. Deferred items (authZ/rate-limit/security-logging) confirmed documented, not silently dropped or implemented.
Decisions: none.
Halts: none (gap was minor, foldable).

## fix — 2026-06-21T06:00:43Z  [✅]
Outcome: Resolved the lone minor gap by aligning artifacts to the (better) actual design — amended spec requirement, design D3, proposal, and tasks 5.2 to "single endpoint, one unified verify→Details→FSM path, NO branch on an untrusted body field (webhookType)". Re-validated: openspec validate passes; grep confirms no remaining webhookType promise in artifacts and no webhookType dependency in code.
Decisions: chose to fix the docs (code was correct) rather than add a spoofable body-field branch.
Halts: none.
Modified files: specs/signing-request/spec.md, design.md, proposal.md, tasks.md.

## manual-test — 2026-06-21T06:00:43Z  [⛔]
Outcome: Implementation complete, all gates + validate green. Halting at the mandatory manual-test gate before archive (archive is never automatic).
Halts: Awaiting user manual-test confirmation. Proposed recipe —
  1. cd backend && ./run-tests.sh  → expect 91 tests, 0 failures (Docker up runs Testcontainers + WireMock).
  2. Boot locally: from repo root `docker compose up -d` (Postgres+MinIO), then `cd backend && ./gradlew bootRun`. Confirm it starts and V3__signing_request migration applies (log: "Migrating schema ... to version 3").
  3. curl checks (no live Leegality needed for the security/validation paths):
     - POST /api/webhooks/esign with `{}` (Content-Type: application/json) → 401 (MAC verify rejects).
     - POST /api/signing/<random-uuid>/request → 404 problem+json (unknown agreement, no provider call).
     - POST /api/signing/not-a-uuid/request → 400 problem+json.
     - Oversized body (>1MiB) to either endpoint → 413.
     - A full create happy-path needs LEEGALITY_BASE_URL pointed at a stub (live sandbox not provisioned — out of scope); the WireMock integration test (SigningRequestApiIntegrationTest) is the stand-in end-to-end and is green.
  Eyeball: no Aadhaar/OTP/signUrl/documentId/webhook-payload appears verbatim in logs (redaction).
  Resume: user confirms working → Archive. Defect → treat under Issue policy (fold minimal fix + re-validate, or split a follow-up CR); do not archive over a known break.

## archive — 2026-06-21T10:50:41Z  [✅]
Outcome: User confirmed manual verification passed. Synced the signing-request delta (all ADDED — new capability) into a new main spec openspec/specs/signing-request/spec.md, then archived the change to changes/archive/2026-06-21-105041Z-create-signing-request. `openspec validate --all` = 10 passed, 0 failed.
Decisions: New capability spec created from delta (no merge needed — no prior signing-request main spec). Agreement stays status-less in the synced spec.
Halts: none.
Follow-up CRs: signing-auth (ownership authZ + rate-limit + redacted security-event logging); reconciliation job (reuses verify→Details→FSM); signed-document download + object storage.
