# Flow journal — bump-spring-boot-security-patches

## propose — 2026-06-20  [✅]
Outcome: Drafted full proposal/design/specs/tasks for CR-8: bump Spring Boot
3.4.2 → 3.4.7 (+ minio 8.5.14 → latest 8.5.x), re-lock, re-scan, and prune the
52 CR-4 OSV suppressions (expiry 2026-07-20) by deletion. Validates --strict.
Decisions:
- Target = 3.4.7 (latest PATCH, same 3.4.x minor) — USER-CHOSEN over 3.5.3 minor
  to minimise blast radius (CR is named "security-patches").
- Modelled as ONE modified-capability delta on `backend-security-scanning`: a new
  "remediated, not renewed" requirement (clear fixable suppressions by upgrade,
  not by extending the expiry). The gate's existing fail-on-any/fail-closed/
  time-boxed requirements are unchanged. No new/other capability.
- Test-task EXEMPT (pure build/dependency change, no runnable behaviour); noted
  atop tasks.md. Verification = full `./gradlew check` regression gate.
- minio is a direct test-scope pin (not BOM-managed) so it needs its own bump;
  assertj + bouncycastle are BOM-managed and clear automatically; Modulith 1.3.4
  carries no finding and stays.
Halts: none (target-version decision resolved up front via AskUserQuestion).
Follow-up CRs: none new (CR-6 frontend-scan, CR-7 CI still pending separately).

## review — 2026-06-20  [✅]
Outcome: One review round (architect/engineer/security). 1 Critical, 7 Major, 5
Minor, 3 Suggestions — ALL in-scope refinements to this CR's own artifacts (no
new CR, no signing/eSign). Folded them; re-validates --strict.
Decisions:
- A2/C3 escape valve: a finding fixed only outside the patch-only policy (3.5.x+)
  or newly-unfixed in 3.4.7 may keep a SHORT-expiry re-justified suppression and
  be called out — not a silent policy break, not a rubber-stamp. Encoded in spec
  carve-out + design decision.
- A1: scoped the new requirement to OSV/dependency suppressions only (SAST
  suppressions explicitly out of scope — fixed by code, not upgrades).
- C1: added per-deletion lockfile-diff cross-check (cleared-because-fixed, not
  spurious). B2/C4: pin minio EXPLICITLY at/above the advisory fixed-version, not
  "latest". B1: explicit Modulith-resolves-clean-on-3.4.7 check. B3: expect
  non-Boot lock churn from lockAllConfigurations. B7: assert named transitives
  moved. C2: durable remediation record = archived CR + journal + toml header.
Halts: none (all findings foldable per Issue policy; escape-valve default obvious).
Follow-up CRs: none new.

## apply — 2026-06-20  [⚠️]
Outcome: Implemented the bump and full regression check is GREEN. OSV findings
52 → 5 (0 Critical / 0 High). Full `./gradlew check` (Testcontainers integration
tests + ModularityTests + JaCoCo + securityScan) passed.
Decisions / HALT resolved:
- HALT (apply): patch-only premise was WRONG (my earlier version data was a stale
  Maven snapshot). Truth at apply: latest 3.4.x = 3.4.13 (not 3.4.7); spring-boot
  self-CVEs GHSA-wwpq-f5c3-7hvx/GHSA-56v8-86gj-66jp have NO 3.4.x fix (only
  3.5.14+); 3.4.13 still ships Tomcat 10.1.50 with three open 9.8/9.1 RCEs.
  Staying on 3.4.x would force muting fixable RCEs — against this CR's own new
  rule. USER CHOSE (AskUserQuestion) → Spring Boot 3.5.15 + Modulith 1.4.12.
- Remediated bcprov 1.81→1.84 via a test pin (minio 8.6.0 pulls it) rather than
  suppress — it's cleanly in our control (spec compliance).
- 5 residual = log4j-core×4 + commons-lang3, ALL bound to the SpotBugs plugin's
  `spotbugs` tool classpath (not app/prod/test-runtime), all Medium. Short-expiry
  suppressed (2026-09-20); clearing needs a SpotBugs-plugin bump → follow-up CR.
- Updated proposal/design/tasks to the 3.5.15 reality (patch-only framing
  superseded with an apply-time-revision banner). openspec validate --strict: OK.
Halts: 1 — resolved as above (3.4.x→3.5.15 minor, user-agreed).
Follow-up CRs: bump-spotbugs-plugin — upgrade SpotBugs Gradle plugin to clear the
  5 tool-classpath log4j/commons-lang3 residuals before 2026-09-20.
Modified files: backend/build.gradle.kts, backend/gradle.lockfile,
  backend/config/osv-scanner.toml (+ CR artifacts proposal/design/tasks/spec).

## validate — 2026-06-20  [✅]
Outcome: Isolated subagent cross-checked artifacts vs the working-tree diff: all
PASS. Versions (Boot 3.5.15, Modulith 1.4.12, minio 8.6.0, bcprov 1.84 test pin)
match; osv-scanner.toml is exactly 52→5 entries (all 2026-09-20, tool-classpath
reasons); build-only (0 src/ files); spec scoped to OSV + escape valve. `openspec
validate --strict` = valid; `osv-scanner` exits 0 (5 filtered, no unsuppressed,
0 Crit/0 High). No gaps → no Fix stage needed.
Decisions: none. Halts: none.

## manual-test — 2026-06-20  [✅]
Outcome: User manually verified and approved ("good"). App boots on 3.5.15 / check
green / OSV clean. Proceeding to archive.
Halts: manual-test gate — resolved (user confirmed).

## archive — 2026-06-20  [✅]
Outcome: Synced the delta (added "Dependency-CVE suppression baseline is
remediated, not perpetually renewed" requirement) into
openspec/specs/backend-security-scanning/spec.md; main spec validates. Archived
to openspec/changes/archive/2026-06-20-052151Z-bump-spring-boot-security-patches/.
Decisions: none. Halts: none.
