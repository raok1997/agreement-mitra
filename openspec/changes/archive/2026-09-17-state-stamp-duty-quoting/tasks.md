> Sequencing (revised 2026-09-16): `stamp-duty-base-calculator` cannot be manually tested without
> this integration, so this change is implemented on top of it **unarchived**. After manual testing in
> the app, archive `stamp-duty-base-calculator` first, then this change.
> `stamp-duty-amount-from-certificate` (not started) rebases onto this one: it takes the next migration
> number after this change's, and its `attachResolved` edit goes after the stamp-value check.

## 1. Telangana rule data (`rules`)

- [x] 1.1 Add `rules/stamp-duty/TG/lease-residential.yaml`: Art. 31 slabs 1-11 / 12-60 per design D2, deposit included, no min/max, counterpart duty INR 50, `registration.requiredWhenTermMonthsOver: 0`, `counselReview: null`, `legalReference` marked UNVERIFIED, each figure's source URL, confidence and open conflict as adjacent comments.
- [x] 1.2 Add `rules/stamp-duty/TG/lease-commercial.yaml`: slabs 1-11 / 12-60 / 61-120 / 121-240 per D2, with the same provenance comments.
- [x] 1.3 Add `rules/stamp-paper/TG.yaml`: `stamp-paper` DENOMINATIONS INR 10/20/50/100 with `maxPapers: 5`, and `challan` ANY_AMOUNT with minimum 0, with sources and confidence.
- [x] 1.4 Add worked `cases:` to both rules covering every slab boundary and the spec scenarios (11-month residential -> 84000 paise; 36-month commercial -> 900000 paise; residential 61 months -> UNSUPPORTED), with expected plans.
- [x] 1.5 Add `boolean reviewed` to `RuleRef` (true iff `counselReview.contentHash` equals the rule hash), and log each rule's id and reviewed flag at startup in `StampDutyConfiguration`. _(Also added `StampDutyCalculator.isChargeable(RuleRef)` and `chargeableStates()`, owned by `rules` next to the `rules.stamp-duty.allow-unreviewed` property, instead of a `chargeable(...)` helper in `signing` (design D3/D4 updated).)_
- [x] 1.6 Unit test: `RuleCasesTest` picks up the TG rules automatically. Add `RuleSetLoaderTest` cases for `reviewed` (matching hash -> true, stale hash -> false, absent -> false). Update the production-defaults integration test in `StampDutyCalculatorIntegrationTest`: TG is now shipped, so an 11-month TG residential basis is Quoted with both media plans.

## 2. Eligibility from the calculator (`signing.agreement`)

- [x] 2.1 Add a package-private `DutyBasisMapper` building `DutyBasis` from `Agreement` + `TemplateDetail` + clock per design D5. An unknown template type or a missing template refuses.
- [x] 2.2 Add a `StampOptions` value (recommended + below-duty single papers) and `chargeable(RuleRef, allowUnreviewed)` per D4/D6, placed where both eligibility and payment can use them. _(The `chargeable` check lives in `rules` (`StampDutyCalculator.isChargeable`), not in `signing`. `StampOptions` needs each medium's denominations, so the public `StampPlan` gained `denominationsPaise`. The evaluation lives in `signing.agreement.StampQuoting`.)_
- [x] 2.3 Rewrite `JurisdictionEligibility.require` / `eligible()` per D4, using the frozen quote at intake and eSign. Delete `JurisdictionProperties`, the `jurisdiction.*` block in `application.yml`, and its test. Add `rules.stamp-duty.allow-unreviewed` (default `false`) with a startup WARN listing unreviewed chargeable rules when true. _(Intake and eSign call `requireForFulfilment`. That path passes a PAID agreement with a frozen quote without checking that its rule id is still loaded, which is simpler than design D4. The allowance and its WARN log live in `StampDutyConfiguration`. `SigningModuleSliceTest` mocks `StampDutyCalculator`.)_
- [x] 2.4 Unit tests: `DutyBasisMapperTest` (execution date fallback in Asia/Kolkata, escalation mapping, usage mapping, refusal on unknown type); `StampOptionsTest` (recommended is the minimum plannable value >= duty with ties in catalog order, below-duty single papers, dedupe, no recommended option when unplannable); `JurisdictionEligibilityTest` (chargeable/unreviewed/allowed, NeedsAdjudication refused, IN refused, frozen-quote path, no template).
- [x] 2.5 Integration test: the finalise, checkout, intake and eSign gates refuse a TG agreement when unreviewed rules are not allowed, and admit it when allowed (the `ConflictException` problem type is unchanged). The jurisdictions picker endpoint lists TG only when chargeable. _(`UnreviewedStampDutyGateIntegrationTest` covers the checkout and finalise refusals and the picker. `JurisdictionGateIntegrationTest` covers the allowed path.)_

## 3. Quote endpoint, pricing and frozen quote (`signing.payment`, `signing.api`)

- [x] 3.1 Add migration `V<next>__stamp_quote.sql` per design D7 (take the next free number at apply time), a `StampQuote` entity and repository, and regenerate nothing else (no dependency change). _(V20. The hash columns are VARCHAR(64), not CHAR(64), so `ddl-auto: validate` accepts them. Added a CHECK that below-duty matches stamp value < duty.)_
- [x] 3.2 Replace `PaymentProperties.Amount` with `Fee(baseMinorUnits, includedStampValueMinorUnits, currency)` bound from `payment.fee.*`, and rework `PaymentPricing` to `price(stampValue)` per D6. Update `application.yml`.
- [x] 3.3 Add `GET /api/agreements/{id}/stamp-quote` (owner-scoped like payment progress) returning the D7 shape, or the frozen quote when an order exists. Add a `warningVersion` constant `under-stamp-v1`.
- [x] 3.4 `POST /payment/order`: accept the D7 body, validate the option and acknowledgement (`400` with the current warning version), and save `PaymentOrder` + `StampQuote` in one transaction. A resumed or settled order ignores the body. _(The choice fails with `StampChoiceInvalidException` (400, `stamp-choice-invalid`, with `reason` + `warningVersion`). `SecurityConfig` permits `GET /api/agreements/*/stamp-quote` and scopes it to the owner in the handler.)_
- [x] 3.5 Add `dutyMinorUnits` / `stampValueMinorUnits` to `CheckoutSessionResponse` and `PaymentProgressResponse`, nullable for orders without a quote.
- [x] 3.6 Unit tests: `PaymentPricingTest` (100 -> 49900, 840 -> 123900, 0 -> 49900); option/acknowledgement validation (value not in options, below duty without ack, stale version, ack on an at-or-above-duty option ignored); the `ack_iff_below_duty` mapping.
- [x] 3.7 Integration tests (Testcontainers): the quote endpoint for an owner vs a non-owner (refused, no amounts disclosed); checkout with the recommended option creates an order whose amount follows the formula and a frozen `stamp_quote` row; an acknowledged below-duty checkout persists ack columns and no PII; the invalid-option and missing-ack paths never call the Razorpay stub; resuming with a different choice returns the original order; changing the rule after ordering leaves progress unchanged. _(`StampDutyCheckoutIntegrationTest`. The existing payment tests now choose an INR 100 stamp value via `support/StampChoices`, which keeps their 49900-paise amounts.)_

## 4. Stamp intake reconciliation (`signing.signingrequest`, staff queue)

- [x] 4.1 In `attachResolved`, after `jurisdiction.require`, refuse `dutyAmount` below the reference value (frozen stamp value, else the recomputed legal duty for a waived payment) with a new `ConflictException.stampValueBelowPaid`; add the `REFUSED_STAMP_VALUE` audit outcome. _(The reference-value selection is `signing.agreement.StampValueReference`. Existing fulfilment fixtures now attach a INR 10,000 certificate, which covers the recomputed duty for waived fixtures.)_
- [x] 4.2 Add `paidStampValue` and `belowDutyChosen` to `StampQueueEntry` and its projection, reading `stamp_quote` only.
- [x] 4.3 Unit test: the reference-value selection (frozen vs waived fallback) and the audit outcome mapping.
- [x] 4.4 Integration tests: a certificate below the paid value gets `409 stamp-value-below-paid` with no blob or stamp; equal to it proceeds; an acknowledged below-duty value of INR 100 with a certificate of INR 100 proceeds; the queue entry shows the paid value and below-duty marker and still has no rent or deposit. _(Added to `StampIntakeApiIntegrationTest` using the `support/StampQuotes.freezePaid` fixture.)_

## 5. Frontend

- [x] 5.1 Add `src/api/stampQuote.ts` (`getStampQuote`) and extend `payForAgreement` in `src/api/payments.ts` to send `{ stampValueMinorUnits, underStampAcknowledgement? }`.
- [x] 5.2 Add `StampQuoteStep.vue` per design D9 (Composition API, `<script setup>`, Tailwind), inserted between `ContactConfirmation` and `finaliseAndPay` in `CaptureForm.vue`, with an unavailable state when `available: false`. _(`views/StampQuoteStep.vue`, also used by the status page's "Complete payment" when no order exists to resume. A frozen order resumes on its stored choice.)_
- [x] 5.3 Unit tests (vitest): recommended is pre-selected; choosing below duty shows the warning and disables pay until acknowledged; the totals shown come from the response, not computed client-side; `payForAgreement` sends the body.
- [x] 5.4 Run `npm run lint` and `npm run build` (scan, test, type-check, build). _(`npm run lint`: 0 errors, 35 warnings (the known prettier/vue-lint overlap, `frontend-vue-lint-warnings`). `npm run build` stops at the fail-closed OSV gate because osv-scanner is not installed locally; its other steps run directly: vitest 238 passed, `vue-tsc -b` clean, `vite build` succeeded.)_

## 6. End-to-end and gates

- [x] 6.1 E2E happy path (extend the existing signing happy-path test): a TG residential agreement with unreviewed rules allowed goes quote -> recommended option -> order -> webhook paid -> intake at the paid value -> `STAMPED`. _(Added `aQuotedStampChoiceIsChargedFrozenAndReconciledAtIntake` to `RazorpayPaymentGateIntegrationTest`: quote, recommended INR 1,300, order at 169900 paise, signed webhook -> PAID, a INR 100 certificate refused with `stamp-value-below-paid`, a INR 1,300 certificate -> STAMPED.)_
- [x] 6.2 `ModularityTests` green with the new `signing -> rules` dependency on the root API only. _(Green: the new `signing -> rules` dependency uses only the root API. `SigningModuleSliceTest` mocks `StampDutyCalculator`.)_
- [x] 6.3 Run `./run-tests.sh` (`check`), reporting wall-clock time against the 3-minute budget, plus `spotbugsMain` and `spotlessApply`. _(2026-09-16: `./run-tests.sh test -Ptest.forks=1 --no-daemon` passed: 136 classes, 1311 tests, 0 failures, 0 skipped. Wall clock 7m37s, over the 3-minute budget (reported). The test classes sum to 153s, up from 83s before this change, and the two new Spring contexts (the checkout and unreviewed-gate property sets) add startup on top. It ran single-fork without a daemon on a memory-starved host. `spotbugsMain`: 0 findings. `spotlessApply` applied. OSV gates not run: osv-scanner is not installed locally.)_

## 7. Docs and close-out

- [x] 7.1 `docs/LEGAL-POSTURE.md` section 1a: mark steps 1-2 implemented (pricing formula, quote before payment, free-text amount retired by `stamp-duty-amount-from-certificate`), step 3 still open; record the counsel gate and allowance.
- [ ] 7.2 `docs/TERMS-OF-SERVICE.md` section 7: add the below-duty choice and its consequence, marked for counsel wording; regenerate any generated copy the jurisdiction spec requires. _(NOT done by the implementation: this is published legal text with uncommitted edits by the user, so a draft paragraph is left for the user or counsel. Tracked as `tos-below-duty-stamp-choice` in the ROADMAP register.)_
- [x] 7.3 CLAUDE.md: note in one line that eligibility comes from reviewed duty rules (the allowlist is gone) and name `rules.stamp-duty.allow-unreviewed`.
- [x] 7.4 `docs/ROADMAP.md`: replace the `state-stamp-duty-quoting` register row with follow-up rows: Karnataka rules and template; property-state choice for national templates; stamp paper + challan combined plans; live vendor stock; refund/absorb on certificate price difference (LEGAL-POSTURE step 3); registration workflow; counsel review of the TG figures, deposit treatment, registration notice and under-stamp warning; the ops question of what staff buy for TG given no SHCIL listing. Remove the stale `jurisdiction-eligibility` purpose-text reference to the allowlist after archive. _(Replaced the change's row with nine follow-up rows: counsel review of TG figures, ToS wording, certificate price reconciliation, KA, national template state, paper + challan, capture defaults for escalation, inventory-aware planning, registration workflow. The jurisdiction-eligibility Purpose text is still pending: it can only be edited after archive.)_
