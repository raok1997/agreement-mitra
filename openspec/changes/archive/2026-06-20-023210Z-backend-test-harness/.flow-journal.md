# Flow journal — backend-test-harness

## propose — 2026-06-20T00:00Z  [✅]
Outcome: Drafted the full proposal set (proposal, spec, design, tasks) for the backend
integration-test harness — Testcontainers (Postgres + MinIO) + Spring Modulith slice-test
wiring + a JaCoCo coverage floor + a harness smoke test. Deliberately excludes any test of
the stubbed signing API; that's deferred to the first signing-feature CR.
Decisions: One CR (build/test infra — no aggregate/endpoint/FSM change). MinIO over
LocalStack (matches prod + docker-compose). Postgres via native `@ServiceConnection`; MinIO
via small manual wiring (no starter). JaCoCo floor is a low regression floor to ratchet
later. Recorded the dev-policy test exemption note atop tasks.md (no runnable production
behavior; smoke test is the integration-level proof).
Halts: none
Follow-up CRs: none (signing integration test belongs to the existing roadmap CR for
create-signing-request, not spun out here)

## review — 2026-06-20T00:00Z  [✅]
Outcome: Ran a 3-persona spec review (architect / senior engineer / appsec). Round 1
surfaced 2 Critical + 5 Major + 5 Minor; all within the harness slice (no signing-flow or
cross-aggregate scope), so folded rather than halted. Round 2 confirmed all 12 resolved,
no new Critical/Major; scrubbed 3 Minor wording residues.
Decisions: build file is build.gradle.kts (Kotlin DSL) — corrected everywhere. Added
spring-boot-testcontainers dep (provides @ServiceConnection). Test schema via a test
profile ddl-auto: create-drop (Flyway deferred to its own CR — keeps this CR test-scoped).
JaCoCo floor set explicit 0.00 (present-but-vacuous, ratchets later). Docker-availability
guard so check skips (not fails) without Docker. Static singleton containers, NOT withReuse.
Modulith slice reframed as mechanism-boots proof only (modules are stubs); slice test
targets the `signing` module (has beans). Container creds/ports + secret-precedence
hardening asserted.
Halts: none — 2 review rounds, under the 5-round cap.
Follow-up CRs: none new (Flyway/migration tooling noted as a future decision, not spun out)

## apply — 2026-06-20T00:00Z  [✅]
Outcome: Implemented the harness and proved it green against real containers. build.gradle.kts:
jacoco plugin + Testcontainers/MinIO test deps + JaCoCo report & 0.00 verification gate wired
into check. Test infra: HarnessTestConfig (@TestConfiguration, imported) with @Bean
@ServiceConnection Postgres + MinIO bean + DynamicPropertyRegistrar + MinioClient;
application-test.yml (ddl-auto create-drop). HarnessSmokeTest (@SpringBootTest, 2 tests) +
SigningModuleSliceTest (@ApplicationModuleTest). CLAUDE.md Commands updated. All 5 tests pass
with Docker; 3 container tests skip cleanly without it; JaCoCo gate green.
Decisions: Switched from the planned static-singleton-via-superclass to bean-based
@ServiceConnection + DynamicPropertyRegistrar shared via @Import — the superclass
@ServiceConnection field did NOT wire under @ApplicationModuleTest (slice DB fell back to dead
localhost). Composition matches the project's composition-over-inheritance convention. Updated
design.md + spec to match.
Halts: none (design refinement was in-scope, anticipated by the M-3 review note).
Follow-up CRs: none.
Modified files: backend/build.gradle.kts, CLAUDE.md, backend/src/test/java/in/agreementmitra/
{HarnessSmokeTest.java, signing/SigningModuleSliceTest.java, support/HarnessTestConfig.java},
backend/src/test/resources/application-test.yml. NOTE: ~13 src/main + ModularityTests files
show Spotless googleJavaFormat churn from the format hook (no behavioral change) — excluded
from this CR's commit; user stashes that diff per the roadmap GOTCHA.
ENV GOTCHA: this machine runs rancher-desktop; tests need DOCKER_HOST=unix:///Users/janu/.rd/
docker.sock (+ TESTCONTAINERS_DOCKER_SOCKET_OVERRIDE=/var/run/docker.sock) and the gradle
daemon must inherit it (run --no-daemon or restart the daemon with the env exported).

## validate — 2026-06-20T00:00Z  [✅]
Outcome: Cross-checked the diff against artifacts in an isolated subagent. No blocking gaps;
every spec requirement and task satisfied in code. Two minor nits, both in application-test.yml.
Decisions: none
Halts: none
Follow-up CRs: none

## fix — 2026-06-20T00:00Z  [✅]
Outcome: Removed the redundant static storage block from application-test.yml (the
DynamicPropertyRegistrar supplies all storage.* at runtime) — this eliminated both nits at once:
the literal test/test credential placeholders and the stale ContainersBase/@DynamicPropertySource
comment. Re-ran check: 5 tests pass, JaCoCo green; openspec validate --strict clean.
Decisions: drop static storage props rather than rewrite them — registrar is the single source.
Halts: none
Modified files: backend/src/test/resources/application-test.yml
