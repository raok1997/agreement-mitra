# Flow journal — signing-completion (CR-4)

## propose — 2026-06-21  [✅]
Outcome: Drafted the full proposal set (proposal, design, 3 spec deltas, tasks) for CR-4
webhook completion intake — per-signer aggregation, signed-artifact storage, and a
reconciliation fallback. `openspec validate` passes; 4/4 artifacts complete.
Decisions:
- Pre-flow (user, via AskUserQuestion): real per-invitee aggregation (not document-level);
  ONE CR (not split); proceed stub-only with live Leegality e2e deferred.
- Capability mapping 1:1 with the three pieces: aggregation MODIFIES `signing-request`;
  `signed-artifact-storage` and `signing-reconciliation` are NEW capabilities.
- D2 aggregation precedence: any REJECTED→FAILED outranks any EXPIRED→EXPIRED; all
  SIGNED→SIGNED; else no-op.
- D3: terminal FSM decision is count-based (correlation-independent); per-row status
  correlated by ordinal (`signing_order`) — a correlation mismatch can't corrupt the FSM.
- D4: download-on-SIGNED decoupled from the transition, idempotent via deterministic keys +
  null-key guard; failed download leaves SIGNED+null-keys, recovered by reconciliation.
- V4 columns: invitee.status, invitee.signing_order, signing_request.signed_pdf_key,
  audit_trail_key (all nullable, additive).
Halts: none (explore skipped — requirement concrete; scope-split already decided as one CR).
Follow-up CRs: none yet (signing-auth + multi-instance scheduler lock noted as out-of-scope).
Modified files: openspec/changes/signing-completion/{proposal,design,tasks}.md +
specs/{signing-request,signed-artifact-storage,signing-reconciliation}/spec.md

## review — 2026-06-21  [✅]
Outcome: Two-round multi-persona review (architect · senior engineer · security). Round 1
surfaced 2 Critical + 7 Major + minors; all folded into design/specs/tasks. Round 2
confirmed all Criticals/Majors RESOLVED with no new Critical/Major. Two trailing minors
(stray-typo, lock-loss-vs-real-error test) also folded. `openspec validate` passes.
Decisions (folded fixes):
- Pin object keys to internal signing_request.id only (never providerDocumentId/agreementId).
- Capture+persist provider per-invitee id at create; ordinal fallback w/ UNIQUE constraint.
- Widen SignedDocument with content type; rename providerRequestId→providerDocumentId.
- Explicit D4 completion control flow (getStatus no-tx → tx_a aggregate/transition →
  download no-tx → tx_b persist both keys atomically); honest idempotency wording.
- Reconciliation: ORDER BY created_at, grace window on SIGNED arm, (status,created_at)
  index, scheduler disabled in test profile; PDF_GENERATED orphans = explicit non-goal.
- SSRF host-pin on provider artifact URL; private create-if-absent MinIO bucket.
Halts: none during review (2 rounds, under the 5-round cap).
Follow-up CRs (recorded as non-goals, not created): PDF_GENERATED orphan recovery;
multi-instance scheduler distributed lock (ShedLock); prod object-store SSE +
block-public-access; signing-auth (pre-existing) blast-radius note.

## apply — 2026-06-21  [✅]
Outcome: Implemented all CR-4 tasks. `./gradlew check` (via run-tests.sh) BUILD SUCCESSFUL —
all tests pass incl. new integration suite, JaCoCo coverage gate, securityScan (OSV over the
now-shipping minio+bcprov + SpotBugs), and ModularityTests green.
Decisions during apply:
- Reused the existing storage.* config + HarnessTestConfig MinIO container/client; production
  MinioClient bean is @ConditionalOnMissingBean so the test harness client overrides it.
- Moved io.minio:minio 8.6.0 + bcprov 1.84 from testImplementation to implementation (shipping);
  regenerated gradle.lockfile; OSV shipping scan passes at the fixed versions.
- getStatus seam now returns DocumentStatusView (per-invitee); applyAuthoritativeStatus reshaped
  to load invitees + aggregate + return Optional<UUID> (the id needing artifact download).
- Added @Scheduled initialDelay so the reconciliation job doesn't auto-fire at boot; test uses it.
- Note: spotless (Write hook) strips imports added before their usages exist — had to re-add
  several imports after writing the usages.
Halts: none (no design issue surfaced; the only failure was an Instant→Timestamp jdbc binding in
a test, fixed in-place).
Modified files: 13 modified (EsignProvider, SignSession, SignedDocument, LeegalityEsignProvider,
SigningRequest, SigningRequestInvitee, SigningRequestPersistence, SigningRequestRepository,
SigningRequestService, application.yml, build.gradle.kts, gradle.lockfile, 5 tests +
application-test.yml); new: BlobStore, DocumentStatusView, InviteeStatus, ReconciliationProperties,
SchedulingConfig, SigningReconciliationJob, storage/{StorageProperties,StorageConfig,MinioBlobStore},
V4__signing_completion.sql, SigningCompletionIntegrationTest, SigningRequestAggregationTest.

## validate — 2026-06-21  [✅]
Outcome: openspec-validate (subagent) confirmed behavioral requirements 1–6 fully satisfied;
found 3 test-completeness gaps (8.6 redaction missing, 8.3 per-invitee-column/UNIQUE half-covered,
all tasks.md boxes unchecked).
Decisions: gaps are minimal/in-scope → fold (no new CR).
Halts: none.

## fix — 2026-06-21  [✅]
Outcome: Closed all 3 gaps. Added downloadDoesNotLogArtifactBytesOrUrls (redaction, ListAppender),
perInviteeStatusOrdinalAndProviderIdArePersisted + duplicateSigningOrderIsRejectedByUniqueConstraint
(per-invitee columns + UNIQUE, pinned to constraint name uq_sri_request_order); checked all 36
tasks.md boxes. Re-validation (subagent) reports all 3 CLOSED, no new Critical/Major. Full
`./gradlew check` BUILD SUCCESSFUL (OSV clean over 161 pkgs).
Halts: none.
