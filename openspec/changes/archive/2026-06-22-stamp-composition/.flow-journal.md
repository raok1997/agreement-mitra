# Flow journal — stamp-composition (CR-6)

## propose — 2026-06-21  [✅]
Outcome: Drafted the full proposal set (proposal + design + specs + tasks) for compositing a synthetic ₹100 Karnataka e-stamp onto the uploaded draft so the eSigned artifact is a stamped instrument. Skipped Explore (requirement fully bounded by prior user discussion).
Decisions:
- Auto-stamp INSIDE createSignRequest, no new endpoint (D1).
- Data-vs-lifecycle split: StampInfo (descriptive data) on Agreement; STAMPED/STAMP_FAILED (lifecycle) on SigningRequest FSM — does NOT reverse CR-2 status-less Agreement (D2).
- StampInfo @Embeddable, all-nullable, internal-only (not on AgreementResponse), V6__stamp.sql nullable cols on agreement (D3).
- StampProvider seam + synthetic Karnataka ₹100 adapter, dutyPaid=false (D4); deterministic BW-series serial from agreement id (D5).
- PDFBox composition not Chromium (D6, preempts reviewer flag); stamped key stamped/{agreement-id}.pdf (D7); procure-if-empty-else-reuse idempotent, short txs before vendor call (D8); no new module cycle (D9).
- Scope: single CR (one state-machine change + supporting data/seam/composition — inseparable).
Halts: none
Follow-up CRs: none new (pre-existing deferred: signing-auth, draft-revision/supersede, PDF_GENERATED-orphan recovery, ShedLock, prod object-store hardening — all documented as non-goals)
Modified files: openspec/changes/stamp-composition/{proposal,design,tasks}.md + specs/{document-stamping,signing-request,agreement-management}/spec.md

## review — 2026-06-21  [✅]
Outcome: Two-round multi-persona review (architect/eng/security). Round 1 surfaced 10 findings (4 Major, 6 Minor) — all within the CR's slice, none scope-crossing, so all folded into the artifacts. Round 2 verified all 10 resolved, no new Critical/Major; verdict ready to implement.
Decisions / folded fixes:
- Key-scoping relabeled as a reasoned divergence from signed-artifact-storage's request-scoping (not "satisfies CR-4 hygiene"); agreement-scoping is the correct instrument-level home, safe under v1 1:1 (design D7).
- Untrusted-PDF parsing fails closed → STAMP_FAILED (new document-stamping requirement + design D10 + signing-request scenario + proposal security checklist + tasks); bounded by CR-5 10 MiB cap + bounded PDFBox memory.
- D8 tx ordering: blobStore.put strictly OUTSIDE the short tx (no tx spans put or vendor call).
- Reuse branch reframed forward-looking/unit-only (dormant under v1 lock-forever).
- dutyPaid provider-authoritative invariant; page geometry (media-box/rotation/landscape) + Standard-14 font; migration SQL types; OSV bias-to-bump-not-suppress; internal accessor not controller-reachable; negative reconciliation test for STAMP_FAILED.
- Cosmetic: reordered D9/D10; documented transient-STAMP_FAILED non-recoverability under v1.
Halts: none (all findings minimal/in-slice)
Follow-up CRs: none new
Modified files: proposal.md, design.md, tasks.md, specs/{document-stamping,signing-request}/spec.md

## apply — 2026-06-21  [✅]
Outcome: Implemented CR-6 end to end. Auto-stamp inside createSignRequest (StampProvider seam + synthetic Karnataka PDFBox composer), STAMPED/STAMP_FAILED FSM, StampInfo data on Agreement, V6 migration, 422 stamp-failed problem+json. Full `./run-tests.sh check` BUILD SUCCESSFUL — tests (unit + Testcontainers integration + WireMock), jacoco, osvScan (165 pkgs, 0 issues — PDFBox 3.0.5 clean), SpotBugs/FindSecBugs, ModularityTests all green. 32/32 tasks done.
Decisions:
- StampResult also carries jurisdiction + denomination (provider is source of truth) — minor refinement of design D4 so the service stays provider-agnostic.
- markStamped(srId, agreementId, info) does attachStamp + transition in ONE tx (REQUIRED propagation), opened strictly AFTER blobStore.put returns (D9 honored).
- Stamp page renders "INR 100" not ₹ (Standard-14 Helvetica has no ₹ glyph); overlay text is ASCII serial.
- Stamp-failed mapped to 422 Unprocessable Entity (draft accepted at upload, unprocessable at stamp time).
- Existing integration drafts were fake "%PDF-..." strings (unparseable) → would now fail stamping; added support/TestPdfs (real PDFBox-generated PDFs) and switched the happy-path drafts to it; the fake string now drives the malformed→STAMP_FAILED test.
Halts: none
Follow-up CRs: none new
Modified files: build.gradle.kts, gradle.lockfile, db/migration/V6__stamp.sql, signing/SignatureStatus.java, signing/agreement/{StampInfo(new),Agreement,AgreementService}.java, signing/signingrequest/{SigningRequest,SigningRequestPersistence,SigningRequestService}.java, signing/stamp/{StampProvider,StampResult,StampSerials,PdfStampComposer,SyntheticKarnatakaStampProvider}(all new).java, StampFailedException(new).java, GlobalExceptionHandler.java, CLAUDE.md; tests: support/TestPdfs(new), signing/stamp/{StampSerialsTest,PdfStampComposerTest}(new), SigningRequestServiceTest, SigningRequestStateMachineTest, SigningRequestAggregationTest, SigningRequestApiIntegrationTest, SigningCompletionIntegrationTest

## validate — 2026-06-22  [✅]
Outcome: Read-only cross-check of code vs proposal/design/specs/tasks. All spec capabilities RESOLVED. Found 4 gaps — all missing tests for already-correct behavior (zero-page defensive test, PII/redaction toString test, dedicated synthetic-provider test) plus one undocumented benign deviation (StampResult superset).
Decisions: none — gaps folded in Fix.
Halts: none

## fix — 2026-06-22  [✅]
Outcome: Closed all 4 validation gaps. Added zero-page composer test; added SyntheticKarnatakaStampProviderTest (dutyPaid=false / jurisdiction / denomination / BW serial / 2-page composite + toString-no-%PDF redaction); documented the StampResult jurisdiction+denomination superset in design D4. Re-ran full `./run-tests.sh check` BUILD SUCCESSFUL; re-validation clean; openspec validate valid.
Decisions: none
Halts: none
Modified files: openspec/changes/stamp-composition/design.md; tests: signing/stamp/PdfStampComposerTest.java, signing/stamp/SyntheticKarnatakaStampProviderTest.java (new)

## manual-test — 2026-06-22  [⛔]
Outcome: Implementation complete, validate + re-validate clean, full `./run-tests.sh check` green. Halting for the mandatory manual-test gate before archive.
Halts: Awaiting user manual confirmation. Proposed recipe — (1) authoritative: `./run-tests.sh check` green (covers stamped-PDF→MinIO, stampInfo persisted, FSM PDF_GENERATED→STAMPED→SIGN_REQUESTED, malformed→422 STAMP_FAILED, reconciliation excludes STAMP_FAILED). (2) optional live smoke WITHOUT Leegality creds: docker compose up -d → ./start_local.sh → POST /api/agreements → POST /api/agreements/{id}/draft with a "%PDF-"-prefixed-but-not-real-PDF → POST /api/signing/{id}/request ⇒ expect 422 application/problem+json type urn:agreementmitra:problem:stamp-failed and signing_request row STAMP_FAILED (fails at the stamp step, before the provider call, so no creds needed). Full live happy-path 201 still needs LEEGALITY_* creds (unchanged blocker; WireMock IT covers it).

## manual-test (resolved) — 2026-06-22  [✅]
Outcome: User manually tested and confirmed it works. Proceeding to archive.
