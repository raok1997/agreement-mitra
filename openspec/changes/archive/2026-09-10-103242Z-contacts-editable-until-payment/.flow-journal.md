# Flow journal — contacts-editable-until-payment

## apply — 2026-09-10T09:19:43.782Z  [⚠️]
Outcome: Resumed a change that was already implemented in an earlier session: 19 of 20 tasks were ticked and the code is committed. I confirmed the backend refusal, the new problem type and the frontend message handling are all really in the code, so there was no coding left to do.
Decisions: Resume point came from the artifact fallback, not the journal — this change predates the journal tool, so there was no .flow-journal.md to read. openspec status showed all four artifacts done and tasks.md partly ticked, which the skill maps to apply.
Halts: None. The one open task, 6.2, is a manual browser check of a failed Razorpay payment; it cannot be driven from a terminal and is the same thing the Stage 6 manual-test gate asks for, so it is carried there rather than treated as unfinished coding.
Follow-up CRs: none
Modified files:
backend/src/main/java/in/agreementmitra/ConflictException.java
backend/src/main/java/in/agreementmitra/GlobalExceptionHandler.java
backend/src/main/java/in/agreementmitra/signing/agreement/AgreementService.java
backend/src/test/java/in/agreementmitra/signing/agreement/AgreementContactsWindowTest.java
backend/src/test/java/in/agreementmitra/signing/contact/ContactGateIntegrationTest.java
frontend/src/api/agreements.ts
frontend/src/api/agreements.test.ts
frontend/src/views/CaptureForm.vue
frontend/src/views/CaptureForm.test.ts

## validate — 2026-09-10T09:54:32.318Z  [⚠️]
Outcome: Ran all three checks. The code does what the change said it would: every scope exclusion held, all four design decisions matched the code, and 17 ticked tasks pointed at real work. Two bookkeeping errors turned up, and one of them hid a test that was not actually testing anything.
Decisions: Backend and frontend checks were run in the spine; spec-conformance and code review ran as isolated read-only subagents given this change's file list explicitly, so neither could wander into the sibling changes sharing this branch. The missing coverage matrix was treated as an observation rather than a gap, per policy, since the change predates that gate.
Halts: Stopped for four decisions and the user settled all of them: repair the vacuous terms-freeze test inside this change; retrofit the coverage matrix now; and raise two follow-up changes (the jurisdiction message plumbing and the frontend dev-dependency refresh) rather than folding either in here. The frontend build is red on a vitest advisory that has nothing to do with this change and blocks every change in the repo until the dependency bump lands.
Follow-up CRs: jurisdiction problem-type plumbing — a customer hitting an ineligible state sees a raw '409' instead of the written explanation; frontend dev-dep refresh — bump vitest and fix two lint errors in the scan shim, which currently fail `npm run build` for every change

## fix — 2026-09-10T09:54:41.513Z  [✅]
Outcome: Repaired the test that was supposed to prove the terms stayed locked. It had been calling a route that requires sign-in without signing in, so it was turned away at the door and never reached the lock it was checking - it would have passed even if the lock had been removed entirely. It now signs in as the owner, sends a complete request, and checks for the exact refusal. Also corrected two task descriptions and added the coverage table.
Decisions: Fixed inside this change rather than deferring it: it is test-only, and it is the guard for the single risk this change carries - that relaxing the contacts rule accidentally relaxed the document rules too. Found a second trap in the same test while fixing it: once the agreement has an owner, the follow-up read also has to be signed in, or it reports a blank address and the check passes for the wrong reason again.
Halts: None. Full backend suite re-run green including the coverage and dependency-scan gates.
Follow-up CRs: none
Modified files:
backend/src/test/java/in/agreementmitra/signing/contact/ContactGateIntegrationTest.java
openspec/changes/contacts-editable-until-payment/tasks.md

## manual-test — 2026-09-10T10:32:31.110Z  [✅]
Outcome: The user drove the change by hand against the local stack and reported it working. The customer who could not retry a failed payment now can: reopening the contact step, correcting an address and reaching checkout all succeed.
Decisions: Confirmed while testing that a paid agreement offers no route back to the contacts screen at all, because the pay button needs a successful save and a finalised agreement refuses one. So the post-payment refusal is close to unreachable through the interface - it defends a background race, not a path a customer walks.
Halts: Resolved. Two unrelated problems surfaced during the drive and were recorded as follow-ups rather than folded in: a save on a finalised agreement shows a raw '409' instead of saying the terms are locked, and the agreements list cannot distinguish awaiting-payment from paid from out-for-signature.
Follow-up CRs: none

## archive — 2026-09-10T10:32:52.781Z  [✅]
Outcome: Folded the change's one revised requirement into the project's spec of record and moved the change into the archive. The rule about when contact details stop being editable is now part of the permanent specification rather than a pending proposal.
Decisions: Checked first that the requirement being revised already existed in the baseline and that neither of the two sibling changes touching this capability had a claim on it, so no ordering constraint applied. Archived through the CLI so the rebuilt specification was validated before anything was written.
Halts: None. Fold totals: 1 requirement modified, nothing added or removed.
Follow-up CRs: none
