# Flow journal — backend-security-scanning (CR-4)

## propose — 2026-06-20  [✅]
Outcome: Drafted the full proposal set (proposal, design, specs, tasks) for the backend security-scanning build gates — a dependency-vulnerability scan (fail on CVSS ≥ 7.0) and SAST via SpotBugs+FindSecBugs (fail on high-confidence), both behind an opt-in `securityScan` Gradle task with checked-in suppression files. `openspec validate` passes.
Decisions:
- Dep scanner left as a review-time choice: design lays out OSV-Scanner (recommended — no NVD key/cache) vs OWASP Dependency-Check (roadmap default but key+cache heavy). Spec/tasks written to hold under either.
- SAST = SpotBugs + FindSecBugs; design flags verifying Java 21 / Spring Boot 3 bytecode compat (risk R1).
- Gate home = separate `securityScan` task, NOT in `check` (keeps local builds fast; opt-in until CI).
- Single capability spec `backend-security-scanning`; dev-policy left unchanged (it documents posture, this CR implements the backend gate).
- tasks.md uses the pure-config test exemption (no runnable behavior).
Halts: none
Follow-up CRs: frontend-security-scanning (CR-6) — frontend dep scan; ci-pipeline (CR-7) — run these gates automatically. Both pre-recorded in the roadmap memory.

## review (round 1) — 2026-06-20  [⛔]
Outcome: Multi-persona review returned 0 Critical / 7 Major / 9 Minor / 9 Suggestions. Dominant theme = assurance: the gate can green-build three ways (opt-in + no CI + scanner-absent-passes).
Decisions: Foldable (apply in explore, no halt): #3 SAST fail-decision must specify mechanism (FindSecBugs SECURITY-category include filter + confidence=high), #4 OSV CVSS-version selection + no-score-advisory behavior, #5 explicit compile→SpotBugs / resolve→dep-scan task graph + detach SpotBugs from check, #6 report-all-severities-fail-on-threshold, time-boxed suppressions, named version pins, declare backend/config/ convention, optional CycloneDX SBOM, pin/verify scanner binary, note stock SAST won't catch never-log-Aadhaar.
Halts: Two real user decisions — (A) dependency scanner choice (D1 open question, must resolve to fix the contract); (B) enforcement posture: keep separate opt-in task (user's original call) made fail-closed, vs wire securityScan into check now (reviewers' unanimous recommendation, since no CI = near-zero enforcement). Asked the user.

## review (round 2 + resolution) — 2026-06-20  [✅]
Outcome: User decided (A) OSV-Scanner, (B) wire securityScan into check now. Folded both decisions plus all foldable round-1 findings into proposal/design/spec/tasks. Re-review verdict CLEAN — all 7 Majors resolved, no new Critical/Major; fixed two cosmetic stale-wording leftovers in proposal. openspec validate passes.
Decisions: Scanner=OSV-Scanner; securityScan wired into check (overrides earlier not-in-check call — roadmap memory updated); fail-closed on missing binary; SAST fails only on FindSecBugs security-category high-confidence via include filter; report-all/fail-on-threshold; OSV CVSS = highest base score, no-score=below-threshold-but-reported; explicit compile→SpotBugs / lock→dep-scan task graph; Gradle dependency locking enabled; suppressions need reason+expiry under backend/config/; SBOM + never-log-Aadhaar custom rules deferred as non-goals (latter a candidate future CR).
Halts: none (resolved)
Follow-up CRs: CR-6 frontend-security-scanning, CR-7 ci-pipeline (promote securityScan into CI), plus a possible PII-lint/custom-rules CR for never-log-Aadhaar (stock SAST won't catch it).

## apply (partial) — 2026-06-20  [⛔]
Outcome: Implemented the gates in build.gradle.kts (SpotBugs+FindSecBugs SAST scoped to SECURITY/HIGH via config/spotbugs-include.xml; OSV Exec task fail-closed; securityScan aggregate wired into check), enabled Gradle dependency locking (gradle.lockfile, 176 deps), and added config/{osv-scanner.toml, spotbugs-include.xml, spotbugs-exclude.xml} baselines. Installed osv-scanner 2.4.0 via brew.
Decisions during apply: OSV-Scanner 2.4.0 has NO native CVSS threshold (verified via scan --help) → user chose Option A: fail-on-any + curated reason/expiry suppressions; spec/design/proposal/tasks updated to match (CVSS≥7.0 language removed). SpotBugs gating via include filter (SECURITY category) + reportLevel HIGH; spotbugsTest disabled.
Halts: First OSV run revealed 52 real CVEs across 17 packages (26 in tomcat-embed-core incl multiple 9.1–9.8 RCE; spring-webmvc, log4j-core, spring-core, postgresql, etc.) — root cause is the outdated Spring Boot 3.4.2 BOM. NOT day-one transitive noise. Halted to ask the user: (A) land the gate now with a short-expiry baseline + dedicated remediation follow-up CR [recommended for scope]; (B) bump Spring Boot in-CR now + suppress residual; (C) hybrid bump to latest 3.4.x now + follow-up for the 3.5 jump. Awaiting decision.
Modified files (so far): backend/build.gradle.kts, backend/gradle.lockfile (new), backend/config/osv-scanner.toml (new), backend/config/spotbugs-include.xml (new), backend/config/spotbugs-exclude.xml (new); openspec artifacts (proposal/design/spec/tasks) updated for Option A.

## apply (complete) + validate — 2026-06-20  [✅]
Outcome: User chose Option A — landed the gate with a 30-day time-boxed baseline (52 IgnoredVulns, expiry 2026-07-20) and recorded remediation as new follow-up CR bump-spring-boot-security-patches (roadmap memory updated, marked URGENT/before-expiry). Implemented fully: securityScan green; `./gradlew check` runs both gates and passes; OSV fail-path verified (exit 1 unsuppressed); SAST fail-path verified (MD5 fixture → spotbugsMain FAILED exit 3, fixture removed); spotbugsTest disabled. CLAUDE.md Scanning section rewritten. All 16 tasks checked. openspec validate clean (+ --strict); validation subagent cross-check = PASS, no gaps.
Decisions: 52 CVEs are real (outdated Spring Boot 3.4.2 BOM), not noise → baseline is explicitly temporary; build will break at 2026-07-20 if not remediated (intentional forcing function).
Halts: none (Option A resolved the apply halt)
Follow-up CRs: bump-spring-boot-security-patches (NEW, urgent, before 2026-07-20); CR-6 frontend-security-scanning; CR-7 ci-pipeline; possible PII-lint CR.
Modified files: backend/build.gradle.kts, backend/gradle.lockfile (new), backend/config/{osv-scanner.toml, spotbugs-include.xml, spotbugs-exclude.xml} (new), CLAUDE.md; openspec change artifacts.

## manual-test gate — 2026-06-20  [⛔]
Outcome: Implementation complete + validate clean. Halting for user manual test before archive (per flow hard gate).
Halts: Proposed test recipe — (1) `cd backend && ./gradlew check` → BUILD SUCCESSFUL, log shows osvScan "Filtered 52 vulnerabilities / No issues found" + spotbugsMain + securityScan, spotbugsTest SKIPPED. (2) Fail-closed: temporarily rename osv-scanner on PATH (or `PATH= ./gradlew osvScan`) → build FAILS with the install message. (3) Dep fail-path: comment out a few osv-scanner.toml entries → osvScan FAILS naming the CVEs; revert. (4) SAST fail-path: drop a class using MessageDigest.getInstance("MD5") into src/main/java → spotbugsMain FAILS; delete it. Awaiting user confirmation.

