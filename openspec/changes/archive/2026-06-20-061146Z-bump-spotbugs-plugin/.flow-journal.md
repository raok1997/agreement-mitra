# Flow journal — bump-spotbugs-plugin (CR-9)

## propose — 2026-06-20T00:00:00Z  [✅]
Outcome: Drafted the full proposal set (proposal/design/specs/tasks) for retiring the 5
tool-classpath OSV suppressions permanently via "Lever B + lock fix": narrow Gradle
dependency locking to shipping+test configs (so build-tool classpaths leave the lockfile
the OSV scan reads), rewrite the lockfile (moving test-classpath commons-lang3 3.17.0→3.18.0),
empty the osv-scanner.toml baseline, and document the scope policy in CLAUDE.md. openspec
validate passes.
Decisions:
  - Chose Lever B (scope out tool classpaths) over Lever A (bump SpotBugs plugin) — user
    agreed; Lever A is a treadmill, scoping retires the false-positive class once. Plugin
    stays at 6.0.26 (explicit non-goal).
  - Pre-propose investigation finding: commons-lang3 is on testCompile/testRuntime (not
    tool-only as the current suppression claims); commons-compress:1.28.0 already requests
    the fixed 3.18.0, so the stale lock is the only thing pinning 3.17.0. Lock rewrite fixes
    it; the suppression reason was factually wrong.
  - Spec delta: MODIFIED "Backend dependency-vulnerability scan gate" in backend-security-
    scanning to make scope explicit (shipping+test, build-tool classpaths excluded) + added
    a scenario for tool-classpath exclusion.
  - Test-task exemption invoked (build/config+docs, no runnable behavior); verification is
    ./gradlew check green with zero suppressions.
Halts: none
Follow-up CRs: none (this IS the CR-9 follow-up from CR-8)

## review — 2026-06-20T00:00:00Z  [✅]
Outcome: Two-round multi-persona review (architect / senior eng / appsec). Round 1: 0 Critical,
5 Major, 5 Minor, 5 Suggestions. Folded all Majors + the high-value Minors/Suggestions into the
artifacts (in-scope, no scope split). Round 2: all 5 Majors verified RESOLVED against the live
lockfile, no new Critical/Major — verdict "ready to apply." Converged in 2 of 5 allowed rounds.
Decisions:
  - Enumerated all 13 lockfile config tags (Gradle 8.12) and bucketed each: in-scope (lock+scan)
    = the 5 classpath configs (compile/runtime/productionRuntime + testCompile/testRuntime);
    excluded (build/dev/tool) = spotbugs, spotbugsPlugins, jacocoAnt, jacocoAgent, developmentOnly,
    testAndDevelopmentOnly, annotationProcessor, testAnnotationProcessor.
  - Locking mechanism pinned: per-config configurations.named(...){ resolutionStrategy.
    activateDependencyLocking() } after plugins apply (lazy-config hazard noted), replacing
    lockAllConfigurations().
  - Lockfile-rewrite assertions added (shared commons-lang3 entry → 3.18.0=testCompile,testRuntime
    with spotbugs tag gone; empty= regenerated; ~25-line shrink expected; log4j-api shipped stays,
    only tool-only log4j-core leaves).
  - Security framing reworked: build-tool CVEs are lower-risk-but-not-zero (build-host supply-chain),
    compensating control = periodic full-lockfile osv audit / eventual plugin bump + revisit-before-
    prod note in CLAUDE.md.
  - Spec delta: also MODIFIED "remediated, not renewed" to recognize scope-removal as a legitimate
    clearing path (deletion need not match a version change when the finding's config leaves scope);
    added a scenario; updated the shrink scenario.
  - Added negative-path verification task (4.3): temporarily downgrade an in-scope dep, confirm the
    gate still goes red, revert.
Halts: none (all findings minimal/in-scope per Issue policy; folded without scope split)
Follow-up CRs: none

## apply — 2026-06-20T00:00:00Z  [✅]
Outcome: Implemented all tasks. Narrowed dependencyLocking to per-config activation of the 5
shipping+test configs; regenerated gradle.lockfile (174→148 lines, all tool/dev entries gone,
log4j-core removed, log4j-api shipped retained); moved test-classpath commons-lang3 3.17.0→3.18.0;
emptied osv-scanner.toml (0 entries) with a rewritten scope-policy header; documented scope policy
+ revisit-before-prod note in CLAUDE.md (and fixed the stale "current baseline" note). Full
./gradlew check green (Testcontainers).
Decisions:
  - APPLY-TIME PIVOT (commons-lang3): the design predicted --write-locks alone would move it to
    3.18.0. It did NOT — the Spring Boot 3.5.15 BOM MANAGES commons-lang3 to 3.17.0, and
    io.spring.dependency-management forces the managed version onto transitives (overriding plain
    Gradle constraints, so the design's `constraints { testImplementation(...) }` fallback would
    not have worked). Fix used: override the BOM version property `extra["commons-lang3.version"]
    = "3.18.0"`. commons-lang3 is test-classpath-only (verified not on any shipping config), so the
    override is test-only in effect; shipped graph untouched.
  - Dropped the design's `lockMode.set(LockMode.DEFAULT)` line — LockMode needs an import and
    DEFAULT is already the default; per-config activateDependencyLocking() is sufficient.
Halts: none (commons-lang3 pivot was minimal + in-scope per Issue policy — same goal/slice, just a
  different mechanism than predicted; folded without halting)
Modified files: backend/build.gradle.kts, backend/gradle.lockfile, backend/config/osv-scanner.toml,
  CLAUDE.md, openspec/changes/bump-spotbugs-plugin/* (artifacts)
Verification: osvScan green (144 pkgs, empty baseline); negative path (commons-lang3→3.17.0 in
  lockfile) correctly FAILS osvScan exit 1 then restored; full ./gradlew check BUILD SUCCESSFUL
  in 24s; SpotBugs plugin unchanged 6.0.26.

## validate + fix — 2026-06-20T00:00:00Z  [✅]
Outcome: Validate (subagent) cross-checked diff vs artifacts: 0 hard gaps, 1 partial — design.md
D2 still described the predicted commons-lang3 fix path, not the BOM-property-override actually
used. Fixed: added an "APPLY-TIME OUTCOME (recorded)" note to design.md D2 documenting that
--write-locks didn't move it (BOM manages it; dep-management overrides plain constraints) and the
BOM version-property override was used instead (test-only in effect). Re-validated: openspec
validate --strict passes; the single partial was exactly this doc-fidelity note, now closed.
Decisions: doc-only fidelity fix, no code change — implementation was already correct.
Halts: none
Modified files: openspec/changes/bump-spotbugs-plugin/design.md
